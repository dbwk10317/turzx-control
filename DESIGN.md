# turzx-control 설계

## 목표

TURZX 9.2" USB 패널에 **Claude·Codex 사용량과 시스템 센서를 띄우는 크로스플랫폼 데몬**.
Go로 만들고 Windows·macOS·Linux에서 동작한다. 개발은 Mac, 운용은 사무실 Windows PC.

테마는 직접 만들어 갈아끼울 수 있어야 한다.

### 첫 테마 — 요구사항만 확정, 디자인은 미착수

**디자인은 아무것도 결정되지 않았다.** 이 문서와 커밋 이력에 등장하는 화면들은 전부
**처리량을 재기 위한 샘플**이며 시안이 아니다. 레이아웃·배치·색·타이포그래피·가독성 처리는
모두 백지 상태다.

**디자인은 별도 마일스톤으로 진행한다.** 구현에 들어갈 때 디자인 작업을 따로 떼어 제대로
수행하며, 그전까지 아래 요구사항 외에는 확정된 것이 없다.

**배경**: 화려한 모션그래픽 영상을 루프 재생한다. Envato Elements 등에서 받은 기성 mp4를 쓴다.
절차적으로 그리지 않는다.

**표시할 지표** (2~3초 갱신). 무엇을 보여줄지만 정한 것이고 어떻게 보여줄지는 정하지 않았다:

| 그룹 | 값 |
|---|---|
| Claude | 5시간 **남은** 사용량 + 리셋까지 남은 시간 |
| Claude | 7일 **남은** 사용량 + 리셋까지 남은 시간 |
| Codex | 5시간 남은 사용량 + 리셋까지 남은 시간 |
| Codex | 7일 남은 사용량 + 리셋까지 남은 시간 |
| 시스템 | CPU 사용률 + 온도 |
| 시스템 | GPU 사용률 + 온도 |
| 시스템 | RAM 사용률 |

데이터 소스는 **사용한** 비율(`used_percentage`)을 주므로 남은 값은 `100 - used`로 계산한다.
`resets_at`은 Unix epoch이므로 남은 시간으로 환산한다.

**RAM 온도는 제공하지 않는다.** DIMM 온도 센서는 서버급 하드웨어가 아니면 없고, Apple Silicon은
통합 메모리라 별도 센서가 없다. RAM은 사용률만 표시한다.

가독성 처리(화려한 배경 위에서 수치를 읽히게 하는 방법), 레이아웃, 색, 타이포그래피는
디자인 마일스톤에서 정한다.

기술적 제약 하나만 미리 적어둔다: 텍스트와 게이지 바는 안티앨리어싱이 따로 필요 없다
(폰트 렌더러가 처리하고 게이지는 직선이다). 곡선·사선을 직접 그리는 경우에만 슈퍼샘플링이
필요하며, 배경을 기성 영상으로 쓰는 한 해당 없다.

이 문서는 한 개발 사이클용이다. 구현이 끝나면 살아남을 규칙만 `CLAUDE.md`로 옮기고 삭제한다.

---

## 1. 장치

| | |
|---|---|
| USB ID | VID `0x1cbe` / PID `0x0092` (PID가 인치를 인코딩: `0x0088`=8.8", `0x0123`=12.3") |
| 클래스 | `bDeviceClass=255` 벤더 특정 |
| 해상도 | 462×1920 세로 네이티브 |
| 내부 | Cortex-A7 + H264 하드웨어 디코더 |

**디스플레이가 아니다.** OS가 모니터로 인식하지 않고 시리얼 포트로도 잡히지 않는다. 이미지를
USB 벌크로 밀어 넣는 장치다. 신호가 없으면 색 로테이션 데모 패턴을 자체 재생한다(고장 아님).

### 프로토콜

```
1. 500바이트 평문: [0]=명령ID, [2]=0x1A, [3]=0x6D, [4:8]=자정 이후 밀리초(LE)
2. DES-CBC 암호화. 키=IV="slv3tuzx", 0바이트 패딩(PKCS#7 아님) → 504바이트
3. 512바이트 프레임에 담고 [510]=161, [511]=26
4. USB 인터페이스 0 벌크 OUT. 이미지는 암호화 헤더 뒤에 인코딩 바이트를 그대로 이어붙인다.
   페이로드 크기는 [8:12]에 빅엔디언 — 헤더의 다른 필드와 엔디언이 반대다
```

명령: 10=sync, 11=restart, 14=brightness(0–102), 101=JPEG, 102=PNG, 121=H264 청크,
122=스트림 상태, 123=스트림 정지.

구현은 `internal/turzx/`에 있고, 업스트림이 만든 패킷과 바이트 단위로 일치하는지
`testdata/golden.json`으로 검증한다(USB 없이 `go test`).

---

## 2. 함정

구현하면서 반드시 알아야 할 것들이다. **네 가지 모두 오류로 드러나지 않는다** — 장치가 `OK`를
응답하면서 화면만 잘못되거나 통신이 서서히 망가진다.

### PNG는 RGBA만 받는다

- **RGB**: 이미지가 세로 1/4로 축소되어 4번 반복되고, 배경이 갱신되지 않아 이전 화면 위에
  밝은 픽셀만 겹쳐 그려진다.
- **팔레트(P)**: 완전히 무시된다. 화면이 전혀 갱신되지 않는다.

따라서 **색 수를 줄여 프레임을 작게 만드는 최적화 경로가 없다.** 프레임 크기는 40KB 안팎으로
사실상 고정이다.

### 응답 읽기를 건너뛸 수 없다

응답을 무시하고 write만 하면 24프레임째에 OUT이 막히고, 장치에 stale 응답이 쌓여(110개 관측)
이후 모든 전송이 타임아웃난다. 복구하려면 IN 큐를 전부 비우고 sync를 다시 보낸다.

### 시작할 때 IN 큐를 비운다

이전 프로세스가 남긴 응답이 있으면 첫 프레임부터 어긋난다.

### flush 타임아웃이 처리량을 지배한다

응답을 읽은 뒤 남은 응답을 비우는 flush가 필요한데, 업스트림 기본값(100ms×5)이 **6배 손해**다
(13.3KB 프레임에서 5.4fps → 3ms에서 12.7fps). flush를 아예 생략하면 6KB 이하만 통과한다.

---

## 3. 실측

**측정 환경은 macOS(M3 Pro) + libusb, USB 허브 경유다.** Windows는 USB 스택이 달라
**flush 타임아웃을 그 PC에서 다시 튜닝해야 한다.** 3ms는 이식 가능한 상수가 아니다.

측정은 업스트림 Python 구현을 레퍼런스로 삼아 수행했다. 다시 재야 할 때는 아래 참고 절의
저장소를 3.10.0 태그로 받아 `library/lcd/lcd_comm_turing_usb.py`를 직접 호출한다.
이때 **2절의 함정을 반드시 지킨다** — RGBA로 인코딩하고, 응답을 매 프레임 읽고,
시작할 때 IN 큐를 비운다.

### 낱장 PNG — 단순한 배경 전용

지표 개수와 밀도를 맞춘 측정용 샘플 화면(수치 4개 + 게이지 + 갱신 시각)으로 잰 값이다.

| 화면 | 프레임 | flush 3ms | flush 10ms |
|---|---|---|---|
| 단색 배경 | 38.1KB | **14.5 fps** (p95 74ms) | 13.0 fps |
| 움직이는 물결 배경 | 41.9KB | **13.5 fps** (p95 81ms) | 12.2 fps |

작은 프레임 참고: 6.3KB에서 32.9fps(flush 없이), 13.3KB→12.7fps, 15.9KB→12.1fps.

단순한 배경이라면 낱장 PNG만으로 13~14fps가 나온다. H264 인코더 의존성이 없다.

**그러나 화려한 배경에는 쓸 수 없다.** PNG는 무손실이라 블러와 부드러운 그라디언트에서
압축이 무너진다. 같은 지표 오버레이를 얹고 배경만 바꿔 **프레임 크기를 측정한** 값이다
(fps는 위 실측을 근거로 크기에 반비례해 환산한 추정이며 직접 재지 않았다):

| 배경 | 프레임 (실측) | fps (추정) |
|---|---|---|
| 단순 물결 | 45.3KB | 12.5 |
| 기하 라인아트 | 50.8KB | 11.1 |
| 그라디언트 메시 + 블러 | 130.3KB | 4.3 |
| 플라즈마 | 137.9KB | 4.1 |
| **네온 파티클 + 블러** | **497.1KB** | **1.1** |

첫 테마가 요구하는 모션그래픽 배경은 이 범위를 한참 벗어난다. **따라서 낱장 PNG는 단순한
배경의 테마와 폴백용이고, 주 경로는 H264다.**

가로로 쓰려면 1920×462로 그린 뒤 `ROTATE_270`으로 462×1920을 만들어 보낸다.

### H264 스트림 — 주 경로

| | 낱장 PNG (102) | H264 스트림 (121) |
|---|---|---|
| 전송 단위 | 프레임당 40KB 안팎 | 청크 202752바이트 |
| 갱신 속도 | 13~14 fps | 25 fps |
| 호스트 비용 | PNG 인코딩 | 실시간 H264 인코딩 |
| 페이싱 | 호스트가 맞춤 | **장치 버퍼가 자동 조절** |

- 462×1920 H264가 그대로 재생된다. 매크로블록 정렬(462는 16의 배수가 아님)은 문제되지 않았다.
- 8초 영상→4.1초, 20초 영상→19.4초 전송. 버퍼가 차면 **전송이 재생 속도로 수렴**하므로
  호스트가 프레임 페이싱을 계산할 필요가 없다.
- **체감은 배경 콘텐츠에 따라 갈린다.** 화면을 빠르게 가로지르는 막대는 "부드럽다기보다
  봐줄만한, 적당한 애니메이션" 수준이었으나, 완만한 그라디언트 모션그래픽은 36초 연속 재생에서
  부드럽게 나왔다. 첫 테마가 쓰려는 종류의 배경에서는 문제가 없다.
- 36초 연속 재생까지 끊김 없이 확인했다.
- 낱장 이미지와 **섞어 써도 안전하다**(영상 재생 직후 이미지 전송 정상).
- **대역폭이 인코딩 속도보다 빡빡한 제약이다.** USB 상한이 약 233KB/s인데 `-b:v 2M`은
  디테일 많은 콘텐츠에서 249KB/s를 낸다. 1.5Mbps(≈187KB/s)로 묶어야 하며,
  `h264_videotoolbox`는 `maxrate`를 줘도 튀므로(노이즈 소스에서 758KB/s 관측) 확인이 필요하다.
  참고로 생성 소스를 직접 인코딩할 때의 여력은 만델브로 기준 실시간의 5.1배였다.
- 업스트림 README는 이 하드웨어에 대해 "no video or storage support for now"라고 적고 있다.
  영상 경로는 업스트림에서 공식 지원되지 않는 코드다. 우리가 직접 검증하며 써야 한다.

**기성 영상 배경 + 지표 오버레이 파이프라인 실측** (462×1920, 25fps, 30초, `-b:v 1500k`).
오버레이는 지표 개수와 대략적 밀도만 맞춘 측정용 샘플이며 디자인 시안이 아니다:

| 인코더 | 실시간 대비 | 대역폭 |
|---|---|---|
| `h264_videotoolbox` | **15.8배** | 145.9 KB/s |
| `libx264` | **10.0배** | 186.7 KB/s |

둘 다 USB 상한(약 233 KB/s) 아래이고 인코딩 여력이 10배 이상이다. ffmpeg가 배경 디코딩·합성·
인코딩을 모두 처리하므로 호스트에서 매 프레임을 그릴 필요가 없다. 참고로 같은 화면을 낱장
PNG로 만들면 458KB로 1fps도 나오지 않는다.

---

## 4. 데이터 소스

둘 다 5시간 창과 7일 창의 사용률을 **퍼센트로** 준다. 구조가 대칭이다.

| | 경로 | 내용 |
|---|---|---|
| Claude | statusline 훅이 stdin으로 받는 JSON | `rate_limits.five_hour.used_percentage` + `resets_at`, `seven_day` 동일 |
| Codex | `~/.codex/sessions/**/rollout-*.jsonl` 의 마지막 `token_count` | `rate_limits.primary`(300분) / `secondary`(10080분) `used_percent` + `resets_at` |

Keychain OAuth나 비공개 엔드포인트는 필요 없다.

### 시스템 센서

**`gopsutil`(v4)로 대부분 통합된다.** 온도만 플랫폼별로 갈린다.

| 항목 | macOS | Windows | Linux |
|---|---|---|---|
| CPU%·메모리·디스크·네트워크·업타임 | gopsutil | gopsutil | gopsutil |
| 온도 | **gopsutil** (sudo 불필요) | gopsutil로는 **부족** | gopsutil (`/sys/class/hwmon`) |
| GPU | 별도 | 별도 | 별도 |

- **macOS(Apple Silicon)에서 온도가 sudo 없이 나온다.** M3 Pro에서 21개 센서를 확인했다
  (gopsutil v4가 IOKit/SMC를 직접 읽는다). 다만 이름이 `PMU tdie1`~`tdie10`, `PMU tdev1`~`tdev8`,
  `tcal`, `NAND CH0 temp` 식이라 **어느 것이 CPU인지 라벨로 알 수 없다.** 매핑 또는 집계
  규칙(최대값·평균)을 정해야 한다.
- **Windows는 보강이 필요하다.** gopsutil의 Windows 구현은 WMI `MSAcpi_ThermalZoneTemperature`
  하나뿐인데, 이는 ACPI 서멀존이라 대부분의 데스크톱 메인보드에서 값이 없거나 CPU 다이가 아닌
  대략적인 존 온도다. 코어별·GPU·VRM 온도는 얻을 수 없다.
  → LibreHardwareMonitor 내장 웹서버(`localhost:8085/data.json`)를 HTTP GET 하는 것이 가장 게으르다.
  CLR 브릿지 없이 표준 라이브러리만으로 끝난다.
- GPU는 어느 플랫폼에서도 통합되지 않는다. NVIDIA는 NVML/`nvidia-smi`, 그 외는 각자 다르다.

수집기 인터페이스는 하나로 두고 플랫폼별 구현을 파일로 나눈다(`internal/source/`). Go 빌드
태그로 분기하면 런타임 분기가 필요 없다.

**신선도.** Claude·Codex 사용량 두 값 모두 해당 머신이 마지막으로 API를 호출한 시점의
**계정 단위 스냅샷**이다.
Claude는 세션이 statusline을 렌더링할 때만 갱신되는 push 방식이라 폴링할 대상이 없다.
집 Mac과 사무실 PC 양쪽에서 에이전트를 돌리므로 한쪽 화면은 **실제보다 낮은 값**을 보여준다.

→ v1은 각 수치 옆에 **스냅샷 시각을 함께 표시**해 낡음이 눈에 보이게 한다. 값을 합치거나
추정하지 않는다. 양쪽이 봉구 서버에 스냅샷을 발행하는 구조는 v2로 미룬다.

---

## 5. 설계

**Go.** 프로토콜 구현이 117줄(테스트 포함 240줄)이라 포팅은 일회성 비용이다. 업스트림 1002줄 중
850줄은 쓰지 않는 기능(MP4/H264 파싱, 장치 내부 저장소)이다. 상주 데몬의 메모리와 사무실 PC에
런타임이 필요 없다는 점이 이득이다. `gousb`·`systray`가 cgo라 크로스컴파일 이점은 없다
(각 OS에서 빌드, PyInstaller와 동일).

**렌더링은 H264 스트림이 주 경로다.** 첫 테마가 모션그래픽 배경을 요구하고, 그것은 낱장 PNG로
불가능하다. 단순한 배경의 테마와 폴백에는 낱장 RGBA PNG 경로를 쓴다. 테마 엔진은 자체 구현하며
업스트림의 YAML 테마 시스템은 쓰지 않는다.

**장치에 레이어는 없다.** 합성은 전적으로 호스트에서 한다.

```
배경 영상(mp4 루프) ─┐
                     ├→ ffmpeg 합성 → H264 인코딩 → 명령 121 청크 스트리밍 (25fps)
지표 오버레이 ───────┘   오버레이는 2~3초마다만 다시 그린다

단순 배경 테마·폴백: 합성 결과 → ROTATE_270 → 462×1920 RGBA → PNG → 명령 102 (13~14fps)
```

**UI는 트레이 아이콘(네이티브) + 설정 화면은 로컬 웹.** 상주·제어는 각 OS 네이티브가 맞고
(Windows 알림영역, macOS 메뉴바), 설정과 테마 편집은 `127.0.0.1` 서버 + 브라우저가 맞다.
Go의 크로스플랫폼 네이티브 GUI가 약한 것도 있지만 결정적인 이유는 **테마 미리보기**다 —
패널에 뿌릴 화면을 브라우저에 그대로 띄워놓고 만들 수 있다. 트레이 메뉴는
열기/일시정지/밝기/종료 정도만 갖는다.

설정 화면은 `net/http`가 서빙하는 평범한 HTML로 만들고 **`@bonggu/ui`를 쓰지 않는다.**
워크스페이스 규칙에 대한 의도적 예외다 — 제품 표면이 아니라 트레이 데몬에 딸린 로컬 설정창이고,
Go 바이너리 하나를 위해 React 스택을 끌어오는 비용이 얻는 것보다 크다. 설정 화면이 제품 표면으로
자라면 재검토한다.

### 모듈 경계

책임을 넷으로 나누고, **의존 방향을 한쪽으로만** 흐르게 한다.

```
cmd/turzx-control/   배선만 한다. 아래 모듈들을 조립해 데몬을 띄운다

internal/metric/     지표 모듈. 이름으로 값을 조회하는 레지스트리.
                     수집기 하나당 파일 하나, 플랫폼별 구현은 빌드 태그로 분기
internal/theme/      테마 엔진. 테마 파일을 파싱해 "렌더 계획"을 만든다.
                     지표를 이름으로만 참조한다
internal/render/     계획을 실제 프레임 또는 H264 스트림으로 합성한다
internal/turzx/      장치 드라이버. 프로토콜과 USB 전송. 바이트만 안다

internal/daemon/     생명주기·주기 스케줄링·설정 저장
internal/ui/         트레이 아이콘 + 로컬 설정 서버. daemon을 조작하는 클라이언트
```

**의존 방향**

```
ui ──▶ daemon ──▶ theme ──▶ metric
                    │
                    ▼
                 render ──▶ turzx
```

- **테마는 장치를 모른다.** 무엇을 어디에 그릴지만 정하고, PNG인지 H264인지는 알지 못한다.
- **드라이버는 지표를 모른다.** 바이트를 받아 보낼 뿐이다.
- **지표는 테마를 모른다.** 이름으로 값을 내줄 뿐 어디에 쓰이는지 모른다.
- 이 셋을 엮는 것은 `daemon` 하나뿐이고, `ui`는 daemon을 조작할 뿐 나머지를 모른다.

**다듬은 지점 넷**

1. **인코딩을 테마에서 떼어 `render`로 옮겼다.** 테마가 PNG/H264를 알면 렌더 경로를 바꿀 때
   모든 테마가 영향을 받는다. 지금도 첫 테마는 H264, 단순 테마는 PNG로 경로가 갈리므로
   이 분리가 실제로 필요하다.
2. **지표를 이름 기반 레지스트리로 둔다.** 테마 파일이 `cpu.temp` 같은 이름을 쓰고 테마 엔진은
   이름만 안다. 새 지표를 추가해도 테마 엔진을 고치지 않는다.
3. **"데몬 관리"를 `daemon`과 `ui`로 나눴다.** 생명주기·스케줄링과 설정 UI는 다른 책임이다.
   나눠 두면 UI 없이 헤드리스로 돌리거나 UI를 교체하기 쉽다.
4. **배선을 `cmd/`가 한다.** 네 모듈이 서로를 직접 찾지 않게 한다.

**테마가 내놓는 "렌더 계획"**

첫 테마는 배경 영상과 오버레이를 ffmpeg가 합성하는 것이 가장 효율적이다. 그런데 테마 엔진이
프레임을 통째로 그리면 그 이점이 사라지고, 반대로 테마가 ffmpeg를 직접 다루면 장치·인코딩과
결합된다. 그래서 **테마는 실행하지 않고 선언한다**:

```
계획 = { 배경: 영상 파일 | 정적 이미지 | 단색,
         오버레이: 지표 스냅샷을 받아 투명 레이어에 그리는 함수,
         갱신 주기: 배경 fps와 오버레이 갱신 간격 }
```

`render`가 이 계획을 실행한다 — 배경 종류에 따라 ffmpeg 파이프라인을 쓸지 낱장 PNG를 만들지
고르고, 결과를 `turzx`에 넘긴다. 테마는 배경이 영상인지 단색인지만 말하고 어떻게 처리될지는
모른다.

**수집 주기와 렌더 주기는 분리한다.** 지표와 오버레이는 2~3초, 배경은 25fps.
오버레이 렌더링 비용이 프레임레이트와 무관해진다.

### 라이선스

GPL-3.0-or-later. 프로토콜 구현이 turing-smart-screen-python(GPL-3.0-or-later)의 파생이다.
새 소스 파일에 SPDX 헤더와 출처 고지를 유지한다.

---

## 6. 미결정

### 사무실 Windows PC에서 확인할 것

- **flush 타임아웃 재튜닝.** 3ms는 macOS 값이다. 프레임 크기를 고정하고 flush 타임아웃만
  바꿔가며 지속 fps를 재면 된다.
- **온도 소스.** gopsutil의 WMI 서멀존이 그 PC에서 값을 주는지 먼저 확인하고, 안 되면
  LibreHardwareMonitor 웹서버를 쓴다. 그 경우 LHM 상시 실행이 배포 전제 조건이 된다.
- **GPU 소스.** 그 PC의 GPU 종류에 따라 결정한다.
- **인코더.** `h264_videotoolbox`는 macOS 전용이다. nvenc/qsv/amf 중 무엇을 쓸지와
  비트레이트 상한이 지켜지는지 확인한다.

### 어디서든 확인할 것

- **오버레이 갱신 방식.** ffmpeg 필터의 오버레이 입력은 한 번만 읽히므로, 2~3초마다 지표를
  바꾸려면 ffmpeg를 재시작하거나(끊김 우려) 프레임을 파이프로 공급해야 한다.
- **스트림을 끊김 없이 무한히 이어붙일 수 있는지.** 파일 단위 반복 전송은 36초까지 확인했으나
  실시간 생성 스트림을 `CMD_STOP_STREAM` 없이 계속 밀어넣는 것은 검증하지 않았다.
- **Apple Silicon 온도 센서 매핑.** `PMU tdie*` 중 무엇을 CPU 온도로 삼을지.
- 프레임레이트를 25보다 높이면 체감이 나아지는지(명령 15).
- ffmpeg 배포 방식. 외부 프로세스 호출이 가장 게으르지만 배포물에 ffmpeg가 딸려온다.

### 디자인 마일스톤에서 정할 것

레이아웃, 배치, 색, 타이포그래피, 그리고 화려한 배경 위에서 수치를 읽히게 하는 방법.

---

## 참고

**장치 프로토콜 — 이 프로젝트의 구현 근거**

- [mathoudebine/turing-smart-screen-python](https://github.com/mathoudebine/turing-smart-screen-python)
  — 프로토콜 구현의 원본. GPL-3.0-or-later. 우리 Go 포팅과 `testdata/golden.json`이 여기서 나왔다.
- [릴리스 3.10.0](https://github.com/mathoudebine/turing-smart-screen-python/releases/tag/3.10.0)
  — TURZX USB 모델(4.6"/5.2"/8"/8.8"/9.2"/12.3") 지원이 들어간 버전. 3절의 실측과
  `testdata/golden.json`이 이 태그를 기준으로 한다.
- [이슈 #727](https://github.com/mathoudebine/turing-smart-screen-python/issues/727)
  — v1.x USB 하드웨어 지원 논의. 8.8"가 VID `0x1cbe` / PID `0x0088`, 이름 `TURZX1.0`으로
  보고되어 우리 장치(PID `0x0092`)와 대조하는 근거가 됐다.
- [하드웨어 리비전 위키](https://github.com/mathoudebine/turing-smart-screen-python/wiki/Hardware-revisions)
- [phstudy/turing-smart-screen-cli](https://github.com/phstudy/turing-smart-screen-cli)
  — 새 TURZX USB 프로토콜을 다루는 별도 CLI 구현.

**제조사**

- [TURZX 공식 사이트](https://www.turzx.com/en/)
- [8.8/9.2/4.6인치 소프트웨어·테마 배포 페이지](https://www.turzx.com/2025/05/26/88_inch/)
  — 벤더 앱 `TURZX_V3.x`와 테마 팩. 테마 폴더와 나란히 `video` 폴더를 쓴다.

**호스트 쪽 라이브러리**

- [shirou/gopsutil](https://github.com/shirou/gopsutil) — 크로스플랫폼 시스템 지표. v4 사용.
- [LibreHardwareMonitor](https://github.com/LibreHardwareMonitor/LibreHardwareMonitor)
  — Windows 온도·팬·전력. 내장 웹서버가 `localhost:8085/data.json`으로 JSON을 제공한다.
- [getlantern/systray](https://github.com/getlantern/systray) — 트레이 아이콘(전 OS).
- [google/gousb](https://github.com/google/gousb) — libusb 바인딩. cgo다.

**배경 소재**

- [Envato Elements — Motion Graphics Backgrounds](https://elements.envato.com/stock-video/motion-graphics/backgrounds)

---

## 7. 구현 착수 순서

설계 단계는 끝났다. 구현은 아직 한 줄도 시작하지 않았다
(`internal/turzx/`의 프로토콜 구현은 포팅 비용을 재려고 먼저 쓴 것이다).

**선행 — 사무실 Windows PC 환경 확인.** 6절의 Windows 항목들이 인코더와 센서 소스를
결정하므로 다른 작업의 전제가 된다. 장치를 그 PC에 연결해 flush 타임아웃을 다시 재고,
gopsutil의 WMI 서멀존이 값을 주는지, 어느 하드웨어 인코더를 쓸 수 있는지 확인한다.

**병렬 — 디자인 마일스톤.** 첫 테마의 레이아웃·색·타이포그래피·가독성 처리를 정한다.
이 저장소에 등장하는 화면은 전부 측정용 샘플이며 참고 대상이 아니다.

**구현 순서.** 의존 방향의 말단부터 올라간다.

1. `internal/metric/` — 지표 레지스트리와 수집기. 장치 없이 개발·테스트할 수 있고
   Claude·Codex 경로는 이미 검증됐다.
2. `internal/turzx/` — 프로토콜 위에 USB 전송을 얹어 드라이버를 완성한다.
   2절의 함정을 코드로 강제한다(RGBA 검증, 응답 읽기, 시작 시 큐 비우기).
3. `internal/render/` — 렌더 계획 실행. ffmpeg 파이프라인과 낱장 PNG 두 경로.
4. `internal/theme/` — 테마 파일 포맷을 정하고 파서와 렌더 계획 생성을 만든다.
5. `internal/daemon/`, `internal/ui/` — 생명주기와 트레이·설정 화면.

**테마 파일 포맷은 아직 정하지 않았다.** 4단계에서 결정한다.
