# turzx-control 설계

TURZX 9.2" USB 패널에 Claude·Codex 사용량과 컴퓨터 센서를 띄우는 크로스플랫폼 데몬.
개발은 Mac, 실제 운용은 사무실 Windows PC. 이 문서는 한 개발 사이클용이며 구현이 끝나면
살아남을 규칙만 `CLAUDE.md`로 옮기고 삭제한다.

## 1. 장치 실측

**모든 수치는 실제 장치에서 측정했다.** 추정값이 아니며 `reference/`의 스크립트로 재현한다.

> **측정 환경은 macOS(M3 Pro) + libusb, USB 허브 경유다.** 운용 대상인 Windows는 USB 스택과
> libusb 백엔드가 달라서, 특히 아래 flush 타임아웃 튜닝값은 **그대로 옮겨 쓸 상수가 아니다.**
> 사무실 PC에서 `reference/measure_flush.py`로 다시 재고 값을 정한다. 첫 Windows 실행이
> 여기 적힌 fps에 못 미치는 것은 회귀가 아니라 아직 튜닝하지 않은 것이다.

| 항목 | 값 |
|---|---|
| USB ID | VID `0x1cbe` / PID `0x0092` (PID가 인치를 인코딩: `0x0088`=8.8", `0x0123`=12.3") |
| 장치 클래스 | `bDeviceClass=255` 벤더 특정. **디스플레이가 아니다** — OS가 모니터로 인식하지 않고 시리얼 포트로도 안 잡힌다 |
| 네이티브 해상도 | 462×1920 세로. 가로로 쓰려면 이미지를 `ROTATE_270` 한 뒤 보낸다 |
| 신호 없을 때 | 색 로테이션 데모 패턴을 자체 재생 (고장 아님) |

### 프로토콜

```
1. 500바이트 평문: [0]=명령ID, [2]=0x1A, [3]=0x6D, [4:8]=자정 이후 밀리초(LE)
2. DES-CBC 암호화. 키=IV="slv3tuzx", 0바이트 패딩(PKCS#7 아님) → 504바이트
3. 512바이트 프레임에 담고 [510]=161, [511]=26
4. USB 인터페이스 0 벌크 OUT. 이미지는 암호화 헤더 뒤에 인코딩 바이트를 그대로 이어붙인다.
   페이로드 크기는 [8:12]에 빅엔디언 — 헤더의 다른 필드와 엔디언이 반대다
```

명령: 10=sync, 11=restart, 14=brightness(0–102), 101=JPEG 업로드, 102=PNG 업로드.

업스트림은 페이로드를 청킹하지 않고 한 번에 write한다(상수 `MAX_CHUNK_BYTES`는 1MB). 이는
관측한 업스트림 동작이며, 장치가 그보다 작은 단위를 요구하는지는 확인하지 않았다.

### 흐름 제어 — 여기서 사고가 난다

- **응답 읽기를 건너뛸 수 없다.** 응답을 무시하고 write만 하면 24프레임째에 OUT이 막히고,
  장치에 stale 응답이 쌓여(110개 관측) 이후 모든 전송이 타임아웃난다. 복구하려면 IN 큐를
  전부 비우고 sync를 다시 보내야 한다.
- **시작할 때 IN 큐를 비워야 한다.** 이전 프로세스가 남긴 응답이 남아 있으면 첫 프레임부터 어긋난다.
- **큰 프레임에는 flush가 필요하다.** 5.5KB 프레임은 응답을 1:1로 읽는 것만으로 120프레임을
  미수신 0으로 완주했지만, 7.6KB 이상에서는 같은 방식이 몇 프레임 만에 실패했고 남은 응답을
  비우자 안정됐다. 응답 개수가 가변인지, 큰 프레임이 단지 처리 시간을 더 쓰는 것인지는
  구분하지 못했다 — 포팅할 때 이 둘을 다른 원인으로 보고 다시 확인한다.

### 처리량 — 프레임 크기가 곧 fps 예산

flush 타임아웃이 지배적 변수다. 업스트림 기본값(100ms×5)이 6배 손해다.

| 프레임 | flush 100ms | flush 3ms |
|---|---|---|
| 5.5KB | — | **39.3 fps** (p95 50ms) |
| 12.4KB | 5.9 fps | **15.5 fps** |
| 14.9KB | 5.8 fps | **14.8 fps** |

인코딩 선택이 그대로 fps가 된다. 대상이 사진이 아니라 UI 그래픽이라 **JPEG는 쓰지 않는다**:

| 배경 | PNG24 | 팔레트64 | 팔레트16 | JPEG q80 |
|---|---|---|---|---|
| 단색 + 도형 | 6.3KB | 2.3KB | **2.3KB** | 23.0KB |
| 그라디언트 물결 | 6.6KB | 7.6KB | **2.6KB** | 26.3KB |
| 풀 그라디언트 | 4.0KB | 11.2KB | **1.5KB** | 24.1KB |

팔레트 16색 PNG로 그리면 1.5~2.6KB로 떨어져 40fps 가까이 나오지만, **색 수를 줄여야 한다는
제약이 붙는다.** 리치한 풀컬러 화면을 이 파이프로 움직이려 하면 프레임이 커져 fps가 무너진다.

### 그래서 낱장 PNG는 주 경로가 아니다 — H264 스트림이 주 경로다

장치 안에 **Cortex-A7 애플리케이션 프로세서**가 있고 H264 하드웨어 디코더를 갖고 있다.
벤더 앱이 테마 폴더와 나란히 `video` 폴더를 쓰는 이유이며, 비디오 재생은 이 제품의 설계 기능이다.
낱장 파이프와는 완전히 다른 경로이고 제약도 다르다.

| | 낱장 PNG (명령 102) | H264 스트림 (명령 121) |
|---|---|---|
| 한 번에 보내는 크기 | 7.6KB 이상에서 실패 | **청크 202752바이트(198KB)** |
| 색 | 팔레트로 줄여야 유리 | 풀컬러 그대로 |
| 페이싱 | 호스트가 맞춰야 함 | **장치 버퍼가 자동 조절** |

**실측(모두 실제 재생 확인):**

- 462×1920 세로 H264가 **그대로 재생된다.** 462가 16의 배수가 아니라 매크로블록 정렬이
  깨질 것을 우려했으나 문제 없었다. 8.8"용 480 해상도로 맞출 필요가 없다.
- 8초 영상 → 4.1초 전송(버퍼가 받아줌), 20초 영상 → 19.4초 전송. 장치 버퍼가 차면
  **전송 속도가 재생 속도로 수렴한다.** 실시간 스트리밍이 구조적으로 성립하며 호스트가
  프레임 페이싱을 계산할 필요가 없다.
- 움직임은 부드럽다(육안 확인). 재생이 끝나면 마지막 프레임에서 정지한다(`CMD_STOP_STREAM`).
- 스트림 흐름 제어는 명령 122로 큐 깊이를 조회해 `response[8] > 3`이면 대기하는 방식이다.

**실시간 합성 여력(462×1920, 25fps, 60초 인코딩):**

| 인코더 | 실시간 대비 | 산출 비트레이트 |
|---|---|---|
| `h264_videotoolbox` (하드웨어) | **18배** | 49 KB/s |
| `libx264` (소프트웨어) | 8.3배 | 245 KB/s |

USB 전송으로 관측한 233KB/s와 비교하면 양쪽 모두 여유가 있다. **매 프레임을 합성해서
실시간 인코딩해 흘려보내는 파이프라인이 성립한다.**

**결론: 리치한 풀컬러 영상이 부드럽게 움직이는 화면이 가능하다.** 낱장 PNG 파이프의
7.6KB·팔레트 제약은 H264 경로에는 적용되지 않는다.

## 2. 데이터 소스 실측

둘 다 5시간 창과 7일 창의 사용률을 **퍼센트로** 준다. 구조가 대칭이다.

| | 경로 | 내용 |
|---|---|---|
| Claude | statusline 훅이 stdin으로 받는 JSON | `rate_limits.five_hour.used_percentage` + `resets_at`, `seven_day` 동일 |
| Codex | `~/.codex/sessions/**/rollout-*.jsonl` 의 마지막 `token_count` 이벤트 | `rate_limits.primary`(300분) / `secondary`(10080분) `used_percent` + `resets_at` |

Keychain OAuth나 비공개 엔드포인트는 **필요 없다**. 초기 검토에서 고려했으나 statusline 경로가
같은 데이터를 더 안전하게 주므로 폐기한다.

### 신선도 — 설계에 반드시 반영

두 값 모두 **계정 단위 한도의 스냅샷**이고, 해당 머신이 마지막으로 API를 호출한 시점의 것이다.
Claude 쪽은 세션이 statusline을 렌더링할 때만 갱신되는 push 방식이라 폴링할 대상이 없다.
집 Mac과 사무실 PC 양쪽에서 에이전트를 돌리므로, 한쪽 화면은 다른 쪽 작업량을 모른 채
**실제보다 낮은 값**을 보여준다. 틀리기에 나쁜 방향이다.

→ v1은 **각 수치 옆에 스냅샷 시각을 함께 표시**해 낡음이 눈에 보이게 한다. 값을 합치거나
추정하지 않는다. 양쪽 머신이 봉구 서버에 스냅샷을 발행하는 구조는 v2로 미룬다.

## 3. 기술 선택과 근거

**Go.** 드라이버 포팅이 일회성 비용이라는 판단을 실측으로 확인했다 — 프로토콜 구현 117줄,
테스트 포함 240줄. 업스트림 1002줄 중 850줄은 MP4/H264 비디오 재생과 장치 내부 저장소
파일 관리라 쓰지 않는다. 상주 데몬의 메모리와 사무실 PC에 런타임이 필요 없다는 점이 이득이고,
`gousb`·`systray`가 cgo라 크로스컴파일 이점은 없다(각 OS에서 빌드, PyInstaller와 동일).

**화면은 코드로 직접 그린다.** 테마 엔진을 자체 구현한다. 업스트림의 YAML 테마 시스템은 쓰지 않는다.

**렌더링은 H264 스트림을 주 경로로 한다.** 장치에 레이어나 오버레이 기능은 없다 — 명령 목록에
없고, 비디오 재생 직전에 `clear_image`를 호출하는 것으로 보아 프레임버퍼를 공유한다. 따라서
합성은 전적으로 호스트에서 한다.

```
배경(영상·절차적 애니메이션) + 센서·사용량 수치
   → 호스트가 462×1920 한 장으로 합성
   → H264 실시간 인코딩(하드웨어 인코더 우선)
   → 청크로 스트리밍(명령 121). 장치는 완성된 화면만 디코딩한다
```

"레이어"는 우리 코드 안에만 존재한다. 낱장 PNG(명령 102) 경로는 **정적 화면과 폴백용으로
유지한다** — 애니메이션이 필요 없는 테마, 인코더를 못 쓰는 환경, 오류 표시에 쓴다.

**포팅 검증은 golden 대조.** 업스트림 Python이 만든 512바이트 패킷을 타임스탬프 0으로 고정해
`testdata/golden.json`에 떠 두고, Go 구현이 바이트 단위로 일치하는지 USB 없이 검증한다.
DES 패딩·키/IV 재사용·`[8:12]` 엔디언·트레일러가 한 번에 걸린다. 9케이스 통과 확인됨.

**UI는 트레이 아이콘(네이티브) + 설정 화면은 로컬 웹.** 상주·제어는 각 OS 네이티브가 맞고
(Windows 알림영역, macOS 메뉴바), 설정과 테마 편집은 `127.0.0.1` 로컬 서버 + 브라우저가 맞다.
근거는 Go의 크로스플랫폼 네이티브 GUI가 완성도·유지보수 면에서 약하다는 점, 그리고 결정적으로
**테마 미리보기가 웹에서 압도적으로 쉽다** — 패널에 뿌릴 1920×462 화면을 브라우저에 그대로
띄워 보면서 만들 수 있다. 트레이 메뉴는 열기/일시정지/밝기/종료 정도의 최소 항목만 갖는다.

설정 화면은 `net/http`가 서빙하는 평범한 HTML로 만들고 **`@bonggu/ui`를 쓰지 않는다.**
워크스페이스 규칙은 웹 UI에 디자인 시스템 적용을 요구하므로 이는 의도적인 예외다 — 근거는
이 화면이 제품 표면이 아니라 트레이 데몬에 딸린 로컬 설정창이고, Go 바이너리 하나를 위해
React 컴포넌트 라이브러리와 빌드 체인을 끌어오는 비용이 얻는 것보다 크다는 판단이다.
설정 화면이 제품 표면으로 자라면 이 예외를 재검토한다.

## 4. 모듈 경계

```
internal/turzx/     프로토콜·USB 전송. 장치만 알고 대시보드는 모른다
internal/source/    데이터 수집기. 하나당 하나의 파일, 공통 인터페이스로 값 하나를 낸다
                    새 데이터 추가 = 파일 하나 추가
internal/theme/     테마. 수집된 값을 받아 462×1920 프레임을 그린다
                    화면 교체 = 테마 교체. 첫 테마는 라이브 배경 + 2~3초 갱신 수치
internal/encode/    프레임 → H264 스트림. 하드웨어 인코더 우선, 정적 테마는 PNG 경로로 우회
internal/ui/        트레이 아이콘 + 로컬 설정 서버
cmd/turzx-control/  데몬 진입점
```

수집 주기와 렌더 주기를 분리한다. 데이터는 2~3초, 배경은 25fps. 전송 페이싱은 장치 버퍼가
알아서 맞추므로 호스트에서 계산하지 않는다.

## 5. 미결정

- GitHub 저장소 public/private (워크스페이스 규칙상 기본값을 가정하지 않는다)
- H264 인코더를 어떻게 호출할지. ffmpeg 외부 프로세스가 가장 게으르지만 배포에 ffmpeg가
  딸려온다. Windows에서 어느 하드웨어 인코더(nvenc/qsv/amf)를 쓸지도 그 PC에서 확인해야 한다
- 스트림을 끊김 없이 무한히 이어붙일 수 있는지. 파일 단위 반복 전송(`loop=True`)은 확인했으나
  실시간 생성 스트림을 `CMD_STOP_STREAM` 없이 계속 밀어넣는 것은 검증하지 않았다
- 사무실 Windows PC의 센서 소스. LibreHardwareMonitor 웹서버(`localhost:8085/data.json`)가
  유력하나 그 PC에서 확인하지 못했다
