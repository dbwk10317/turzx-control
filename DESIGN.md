# turzx-control 설계

**상태: G0/G1 검증 도구 구현 중, 제품 데몬 미구현.** 정적 출력 가능성은 확인됐고 유한 H264
검증 경로는 단위 테스트와 Windows 장치 응답, 사용자의 전체 화면 정상 재생 확인까지 마쳤다. 실시간 표시 지연과
Windows 운용 전체는 검증되지 않았다. 아래 검증 관문의 통과를 뜻하지 않는다.
검토 기준: `40ceee7`의 코드·문서·측정 스크립트 이력과 2026-09-10에 확인한 외부 자료.

## 1. 평가와 범위

**컨셉은 타당하다. Go 데몬 + 호스트 합성 + USB H264 전송을 유지하되, 실시간 경로의 검증을
첫 단계로 옮긴다.** 센서·폰트·영상·USB 접근은 공개 라이브러리를 활용하고, 이 프로젝트는
지표 정규화·테마 바인딩·장치 프로토콜·전송 복구에 집중한다.

| 우선순위 | 리뷰 결과 | 반영한 판단 |
|---|---|---|
| 착수 전 검증 | 파일 재생과 실시간 지표 합성은 다르다. 전송 완료는 화면 표시 완료가 아니다 | 연속 스트림·오버레이 교체·실제 표시 지연을 함께 검증 (§5, G1) |
| 착수 전 검증 | 업스트림은 청크 협상·상태 조회·마지막 청크 표시를 쓴다. Go에는 영상 명령 구현이 없다 | 영상 전송 계약과 복구 경계를 정의 (§6) |
| 착수 전 검증 | Windows는 flush 튜닝 이전에 USB 드라이버 바인딩과 일반 사용자 접근이 성립해야 한다 | Windows + WSL의 역할과 설치 조건을 먼저 확인 (§4, G0) |
| 구현 시 필수 | Go 기본 PNG 인코더는 불투명 RGBA를 RGB로 저장한다 | 인코딩 결과 IHDR color type 6을 보장 (§6) |
| 구현 시 필수 | 누락·리셋 경과·여러 계정·여러 한도 버킷을 구별할 계약이 없다 | 값과 출처·시각·상태를 함께 취급 (§4) |
| 설계 수정 | Codex에 공식 App Server 조회가 있다 | 공식 조회를 우선, 로그는 명시적 대체 경로 (§4) |
| 근거 정정 | 짧은 전송 실측과 오프라인 인코딩 속도를 전체 시스템 보장으로 확대했다 | 과거 관측·현재 코드 검증·추론·목표를 분리 (§2, §3, §8) |

### 제품 요구사항

- 보유한 **TURZX 9.2인치 한 대**, 논리 화면은 가로 **1920×462**.
- 개발은 macOS, 일상 운용은 사무실 Windows PC. 사용자는 **Windows와 WSL 양쪽에서
  Claude·Codex를 사용**한다. Linux도 같은 구조를 지원하되 OS별 검증 전에는 지원 완료로 표기하지 않는다.
- v1 첫 테마의 AI 사용량은 Windows 트레이 데몬이 관리하는 공급자별 전용 프로필만 표시한다.
  기존 Windows 앱·CLI 프로필과 WSL 계정·버킷은 선택·합산·대체하지 않는다.
- 첫 테마는 기성 mp4 모션그래픽 배경을 반복하고 지표를 겹친다. 배경은 절차적으로 만들지 않는다.
- Claude와 Codex 각각 5시간·7일 **남은 사용량과 리셋까지의 시간**, CPU·GPU 사용률과 온도,
  RAM 사용률과 온도를 표시한다. RAM 온도 센서가 없으면 메인보드 온도로 대체하며 실제 출처를
  `메인보드 온도`로 표시한다. 둘 다 없으면 미지원이다. 계정에서 해당 창을 제공하지 않으면 미제공 상태로 표시한다.
- 하드웨어 지표 수집과 전체 화면 합성은 1초마다 갱신하고, Claude·Codex 사용량 원본 조회는
  30초마다 수행한다. 영상은 우선 25fps다. AI 값은 조회 완료 때 최신 불변 스냅샷으로 원자 교체하고
  매초 합성에서 그대로 읽는다. Claude statusline 원본 수신은 이벤트 기반이며, 반복 합성만으로
  수신 시각·신선도를 갱신하지 않는다.
- 사용자가 테마 파일·소재를 교체할 수 있어야 한다.

v1은 단일 장치, 공급자별 선택 계정 하나, 영상 테마 하나와 정적 폴백, 트레이 제어,
로컬 설정·정적 미리보기를 제공한다. 범용 위젯 플러그인, 브라우저 테마 편집기, 다중 장치,
과금·토큰 비용 추정, 원격 제어, Mac↔사무실 스냅샷 중계 서버는 뒤로 미룬다.

**첫 테마의 코드 시안을 구현했다.** 제공된 `azure-ribbon.mp4` 위에 하나의 반투명 레일을 두고
시계, `AI 에이전트`, `하드웨어 모니터` 영역으로 묶는다. AI 영역은 Codex·Claude의 5시간/주간 잔여량과
리셋 시간을 이중 진행 막대로, 하드웨어 영역은 CPU·GPU·RAM 사용률과 온도를 표시한다.
색은 짙은 청록 바탕, cyan·blue 강조, 밝은 본문과 회청색 보조 텍스트를 사용한다. 글꼴은
SIL OFL 1.1의 Pretendard 1.3.9 Regular/SemiBold를 소스에 동봉해 OS 설치에 의존하지 않는다.
`-theme azure-ribbon`의 값은 `미리보기 데이터`로 표시하는 레이아웃 검증값이다.
H264 합성 프레임은 확인했지만 실제 패널 가독성과 실데이터·오래됨 상태 연결은 남아 있다.

## 2. 현재 구현과 근거의 범위

| 근거 | 확인된 범위 | 확인되지 않은 범위 |
|---|---|---|
| `internal/turzx/packet.go`, `internal/turzx/testdata/golden.json` | sync·밝기·PNG/JPEG 헤더 암호화와 페이로드 배치 | USB 송수신, 응답 해석, H264, 실제 PNG 형식 검증, 데몬 |
| 기존 Go 테스트 | 본 리뷰에서 `go test ./...` 통과. 기준 패킷과 바이트 단위 비교 | 장치·Windows 호환성을 뜻하지 않는다 |
| `internal/turzx/png.go`와 테스트 | 462×1920·8bit RGBA 강제, 불투명/투명 픽셀 보존, 손상·크기·형식·길이 검사 | 실제 패널 출력·인코딩 처리량 |
| `scripts/check-windows.ps1` | Windows OS·CPU/GPU·도구 경로·대상 USB 바인딩의 읽기 전용 진단 | libusb 로딩·일반 사용자 송수신·센서 수집 |
| `cmd/turzx-probe`, `internal/turzx/usb.go` | Windows 일반 사용자 권한에서 장치 열기·bounded drain·sync·정적 PNG 전송/응답, 사용자가 네 색상 띠의 전체 화면 정상 표시 확인. 유한 H264 단위 테스트와 Windows 장치 전송·정지 응답, 사용자의 진단 영상 전체 화면 정상 재생 확인 | H264의 마지막 프레임까지 재생 완료, 반복 전송 안정성·절전/분리 복구 |
| 기존 PNG 실측 | Mac M3 Pro + libusb + USB 허브, 미리 인코딩한 프레임 전송 | 실시간 그리기·인코딩 포함 성능, 장시간 안정성 |
| 기존 H264 기록 | 파일 재생, 36초 연속 관찰, 고정 오버레이 합성·인코딩 처리량 | 실시간 오버레이 교체, 무한 스트림, 재연결, Windows |
| 공식 문서·업스트림 소스 | 데이터 필드와 참조 구현의 동작 | 설치된 모든 버전·하드웨어에서의 동작 |

측정 도구는 현재 트리에 없다. PNG 측정 방법은 이 저장소 `8526b86^:reference/measure_dashboard.py`
등의 이력에서 확인할 수 있다. 해당 스크립트는 **40장**을 사전 인코딩한 뒤 USB write/read/flush만
재며, KB는 바이트를 1024로 나눈 값이다. 기존 설명의 게이지·갱신 시각과 달리 이 스크립트의
화면은 네 그룹의 라벨·값과 배경이다. 첫 테마 전체 밀도를 재현했다고 보지 않는다.

H264 기록에는 재현에 필요한 전체 명령·인코더 옵션·원본 소재·로그가 모두 남아 있지 않다.
기존 관측은 보존하지만 본 리뷰에서 재측정한 수치로 취급하지 않는다. 다음 검증에는 최소 재현 도구와
소재 해시·버전·옵션·시간 구간을 검사 산출물로 남긴다. 응답 읽기를 생략하는 과거 실험은 재실행하지 않는다.

## 3. 장치와 기존 실측

| 항목 | 확인 내용 |
|---|---|
| USB | VID `0x1cbe`, PID `0x0092`, 벤더 클래스 `255`, 인터페이스 0의 벌크 IN/OUT |
| 패널 | 세로 네이티브 462×1920. OS 모니터나 시리얼 포트로 인식하는 장치가 아니다 |
| 표시 방향 | 1920×462 합성 결과를 시계 방향 90도 회전. 기존 Pillow `ROTATE_270`과 같은 방향 |
| 영상 | H264 디코딩과 네이티브 크기 파일 재생을 관찰. 내부 SoC 상세는 제품 구현의 전제로 삼지 않는다 |
| 무신호 | 기존 관측에서는 색 로테이션 데모를 자체 재생 |

PID에서 임의 모델의 크기·능력을 추론하지 않는다. 보유 장치만 허용 목록에 두며 다른 모델은
별도 확인 후 추가한다. 밝기 벤더 범위는 0–102다.

### PNG

**RGBA PNG만 정상 출력된 것으로 기록돼 있다.** RGB는 축소·반복·잔상, 팔레트 PNG는 갱신 없음이
관찰됐다. 응답 성공만으로 화면 정상 여부를 알 수 없다. 색상 수를 줄인 뒤에도 RGBA를 유지하는
압축 개선까지 불가능한 것은 아니며, 프레임 크기가 40KB로 고정되는 것도 아니다.

| 측정용 화면 | 평균 크기 (KiB) | flush 3ms | flush 10ms |
|---|---:|---:|---:|
| 단색 배경 | 38.1 | 14.5fps, 전송 p95 74ms | 13.0fps |
| 움직이는 물결 | 41.9 | 13.5fps, 전송 p95 81ms | 12.2fps |

작은 프레임의 기존 기록은 6.3KiB에서 32.9fps(flush 생략 실험), 13.3KiB에서 12.7fps,
15.9KiB에서 12.1fps다. **작아도 flush 생략을 제품 경로로 채택하지 않는다.**

배경 복잡도를 바꾼 별도 기록의 크기는 단순 물결 45.3KB, 기하 라인아트 50.8KB,
그라디언트 메시·블러 130.3KB, 플라즈마 137.9KB, 네온 파티클·블러 497.1KB다.
이 표본의 단위는 기존 기록 표기를 유지한다. 크기에 반비례해 계산했던 fps는 직접 실측이 아니므로
성능 기준에서 제외한다. 첫 테마는 H264, PNG는 단순 배경과 폴백이라는 선택은 유지한다.

flush 100ms에서 3ms로 바꾼 13.3KiB 사례는 5.4→12.7fps, **약 2.35배**다.
업스트림 flush는 최대 5회 읽되 타임아웃 때 중단하므로 매번 500ms를 기다리는 구조도 아니다.
3ms는 Mac의 측정 조건이며 Windows 공통 상수가 아니다.

### H264

- 462×1920, 25fps로 인코딩한 파일의 재생을 확인했다. 가로 462가 16의 배수가 아니어도
  해당 파일은 재생됐다. 모든 프로파일·인코더 설정이 호환된다는 뜻은 아니다.
- 8초 영상의 전송 4.1초, 20초 영상의 전송 19.4초가 기록돼 있다. **선행 버퍼링의 증거**이며
  실제 표시 지연이나 자동 페이싱의 충분성을 검증한 결과가 아니다.
- 36초 연속 관찰에서 완만한 그라디언트는 부드러웠고, 빠르게 가로지르는 막대는 덜 부드러웠다.
  재생 후 PNG 출력도 정상 관찰됐다. 임의 시점의 동시 명령·무한 반복 전환은 별도 검증 대상이다.
- H264 경로에서 약 233KB/s의 처리량과, `-b:v 2M`에서 249KB/s, VideoToolbox 노이즈 소스에서
  758KB/s가 기록돼 있다. **233KB/s를 USB 자체의 상한으로 일반화하지 않는다.** 위 PNG 결과만 해도
  38.1KiB×14.5fps ≈552KiB/s다. 모드·장치 버퍼·측정 조건을 나눠 재야 한다.

기성 영상 + 고정 측정용 오버레이, 462×1920, 25fps, 30초, `-b:v 1500k`의 기록:

| 인코더 | 오프라인 처리 속도 / 실시간 | 평균 출력량 (기존 표기) |
|---|---:|---:|
| `h264_videotoolbox` | 15.8배 | 145.9KB/s |
| `libx264` | 10.0배 | 186.7KB/s |

같은 화면의 PNG는 약 458KB였다. 이 기록은 H264 선택의 근거지만 실시간 합성 중 CPU 사용률이나
지표 변경 지연의 근거는 아니다. 다음 측정은 bytes/s와 bit/s, 평균과 짧은 구간 피크를 구분한다.

## 4. 데이터 계약과 Windows + WSL

### 실행 위치

**Windows 네이티브 데몬 한 개가 USB·트레이·센서·렌더러를 소유한다.** WSL의 CPU/RAM을
호스트 센서로 쓰지 않으며 USB를 WSL로 넘기는 구성을 기본으로 하지 않는다.

- v1 표시 입력은 데몬 전용 Windows Claude 프로필의 statusline 어댑터와 데몬 전용 Windows
  Codex App Server로 한정한다. 기존 Windows 앱·CLI 프로필과 WSL 입력은 첫 테마에서 읽지 않는다.
  이후 WSL 지원을 다시 추가해도 WSL localhost와 Windows localhost가 같다는 가정으로 수신 서버를 열지 않는다.
- inbox 파일은 공급자·명시적 계정 별칭·실행 환경·세션별로 분리한다. 같은 디렉터리의 임시 파일을
  쓴 뒤 원자 교체하며, Windows 파일 잠금·WSL 경계에서의 교체 동작은 G0에서 확인한다.
- 설정 UI의 공급자별 `연결` 동작이 브라우저 로그인을 시작한다. 데몬은 사용자 전용 앱 데이터 아래에
  별도 `CODEX_HOME`과 `CLAUDE_CONFIG_DIR`을 두고 해당 공식 프로그램만 이 경로를 사용하게 한다.
  제품 코드는 인증 파일을 읽거나 토큰을 자체 설정 파일로 복사하지 않는다.
- 기존 Windows 앱·CLI와 WSL의 홈은 탐색하지 않는다. 사용자가 같은 공급자 계정으로 기존 클라이언트를
  사용하면 서버 쪽 계정 한도에는 함께 반영되지만, 로컬 관측과 세션을 합산해서 사용량을 만들지 않는다.

### 계정 연결과 전환

선택 계정과 각 입력 소스의 연결에는 로컬 `binding_id`를 부여한다. 이는 인증 정보나 계정 ID가
아니라 **사용자가 확인한 연결의 세대**다. 최초 연결·계정 변경·수동 재연결 때 새 값을 발급한다.
관측·캐시·진행 중 조회는 시작 당시의 `binding_id`에 묶고, 전환 시 이전 세대의 현재 값과
리셋 이력을 비운다. 이전 세대 파일과 늦게 완료된 응답은 현재 값으로 채택하지 않는다.

- Codex는 전용 App Server 시작 시와 각 사용량 조회 전후 `account/read`로 설정 UI에서 연결한 계정 정보를 대조한다.
  `account/updated` 알림은 재확인 계기로 사용한다. 인증 모드가 같은 계정 교체를 이 알림만으로
  감지한다고 가정하지 않는다. 조회 중 계정 정보가 달라지거나 확인이 실패하면 해당 사용량
  응답을 보류하고 `계정 확인 필요` 또는 `인증 필요`로 표시한다. [공식 계약][codex]
- 전용 Windows Codex 계정은 로그인·로그아웃 알림, App Server 재시작과 사용량 조회 전후에
  `account/read`로 확인한다. 계정 변경을 감지하면 기존 연결 세대의 값과 리셋 이력을 즉시 비우고
  새 사용량 조회를 시작한다. 새 응답이 다음 1초 합성 틱까지 완료되지 않으면 `동기화 중`을 표시하며
  이전 계정 값을 새 계정 값처럼 유지하지 않는다.
- 반환되는 계정 정보로 이메일 부재·워크스페이스 변경 등을 구별할 수 없는 설치 환경은
  자동 계정 식별을 지원한다고 표시하지 않는다. G0에서 실제 CLI 버전의 식별 범위와 외부
  로그인 변경 반영을 확인하고, 불충분하면 계정 전환 후 수동 재연결을 운용 조건으로 둔다.
- Claude statusline은 계정 별칭만으로 실제 로그인 계정을 검증할 수
  없다. 사용자가 해당 환경에서 계정을 바꿀 때 수집을 중지하고 로그인 대상을 다시 확인한 뒤
  소스를 재연결한다. UI에 `수동 확인한 계정`임을 표시한다. 통보 없는 로그인 변경까지
  자동 감지한다고 약속하지 않는다.
- 전용 Windows Claude 프로필은 새 로그인 뒤 새 statusline 이벤트에서 계정 전환을 확인한 다음 연결 세대를
  바꾼다. 유휴 상태에서는 이벤트가 없을 수 있으므로 다음 합성 틱의 자동 전환을 보장하지 않으며,
  확인 전에는 `계정 확인 필요`를 표시하고 새 이벤트 또는 수동 재연결을 기다린다.
- Claude 어댑터의 `binding_id`는 세션 시작 시 고정한다. 재연결 때 기존 세션을 종료하고 새
  세션으로 시작한다. 오래된 훅이 현재 설정의 새 세대를 읽어 자신을 재등록해서는 안 된다.
  로그 대체 모드도 재연결 후 새 세션만 허용하며 이전 세션의 뒤늦은 이벤트는 제외한다.

### inbox 수신과 재시작

어댑터 파일에는 허용된 사용량 필드 외에 `schema_version`, `binding_id`, `session_id`,
세션 내 단조 증가 `sequence`를 넣는다. 공급자·환경·별칭은 설정과 파일 경로에 일치해야 한다.
같은 세션의 훅 쓰기는 직렬화하고 순번을 유지한다. 순번을 유지할 수 없는 새 세션은 새 ID를
쓴다. 훅 재실행마다 순번을 0으로 되돌리지 않는다.

- 데몬이 각 세션 파일을 **처음 발견할 때는 과거 스냅샷**으로 읽고 그 순번을 기준으로 잡는다.
  이는 시작 스캔과 실행 중 늦게 발견한 파일 모두에 적용한다. 원래 수신 시각이 없으면
  `received_at=null`이며 `마지막 수신 불명`을 표시한다. 처음 읽은 시각으로 정상 관측을 만들지
  않는다. 새 세션도 첫 파일 이후 다음 훅 쓰기부터 현재 관측으로 채택하는 보수적 정책이다.
- 같은 연결·세션에서 더 큰 순번의 완전한 파일을 읽었을 때만 새 훅 수신으로 인정한다.
  반복 스캔·동일 파일 재읽기·중복 파일 이벤트·작은 순번은 신선도를 갱신하지 않는다.
  내용이 같아도 새 순번이면 훅 수신 시각은 갱신하되 `last_changed_at`과 원본 시각은 유지한다.
- 유효 파일을 읽고 검증한 시각을 `received_at`으로 기록한다. 이는 로컬 수신 시각이며 원본
  API 관측 시각이 아니다. mtime은 변경 탐색에만 사용하고 유효 순번 확인을 대신하지 않는다.
  부분 JSON·임시 파일·잘못된 스키마·다른 연결 세대는 현재 값과 수신 시각을 바꾸지 않는다.
- 데몬 재시작 시 파일을 다시 기준 스냅샷으로 잡는다. 과거 값은 오래됨으로만 표시하며,
  이미 리셋 시각이 지났다면 아래 상태 규칙의 `갱신 대기`를 적용한다. 새 순번을 받기 전에는
  정상 상태로 복귀하지 않는다. 원본 이벤트 시각을 가진 로그 대체 모드도 재탐색 시각으로
  원본 시각이나 신선도를 덮어쓰지 않는다.

### 정규화할 값

각 지표는 `id`, `value|null`, `unit`, `source`, `state`를 갖는다. AI 사용량에는 선택 계정,
한도 버킷, 창 길이, `used_percent`, `resets_at`을 추가한다. 서로 다른 공급자·계정·버킷의
스냅샷은 **합산·평균·최댓값 병합하지 않는다**. 같은 범위의 최신 유효 관측을 선택한다.

시간은 `source_observed_at`(원본이 제공하는 경우), `received_at`(확인된 새 수신 시각),
`last_changed_at`(확인된 내용 변경 시각)을 구분하며 모르면 null로 둔다. 파일 mtime·렌더 시각·
훅 실행 시각으로 원본 관측 시각을 만들어내지 않는다. 출처별로 시각의 의미도 함께 보관한다.

| 표시 상태 | 동작 |
|---|---|
| 정상 관측 | `clamp(100 - used_percent, 0, 100)`을 남은 비율로 표시. 누락과 숫자 0을 구분 |
| 오래됨 | 마지막 유효 값에 오래됨 표시와 출처 시각을 붙임. 새 수신만으로 원본이 새로워졌다고 주장하지 않음 |
| 리셋 시각 경과 | `갱신 대기`로 전환. 100%로 복구하거나 이전 수치를 새 창의 값으로 재사용하지 않음 |
| 미제공·미연결·계정 확인 필요·인증 필요·수집 오류 | 현재 값은 `—`와 원인. 이전 값을 남기면 반드시 과거 값으로 구별 |

상태는 공급자·연결 세대·계정·버킷·창 길이별로 판정한다. 아래 순서로 우선순위를 적용한다.

1. 계정 전환·연결 해제는 이전 세대의 값과 창 이력을 무효화한다. 계정 확인 실패·인증 실패·
   명시적 수집 오류는 해당 원인을 우선 표시한다. 오류 동안 같은 연결의 리셋 이력은 보존하고,
   오류가 해제되면 아래 조건을 다시 평가한다. 출처가 지원 종료를 명시적으로 확인한 경우에는
   창 이력을 끝내고 `미제공`으로 전환한다. 단순 누락은 지원 종료의 증거가 아니다.
2. 이전 유효 창의 `resets_at`이 지났고 새 창을 받지 못했으면 `갱신 대기`다. 이후 성공한
   입력에서 그 창이 누락되거나 null이어도 이 상태를 유지한다. 오래됨 임계값 경과나 훅 수신
   반복만으로 `미제공` 또는 정상으로 바꾸지 않는다.
3. 같은 범위에서 이전 값보다 뒤의 `resets_at`을 가지며 현재 시각에도 유효한 창과 유효 사용률을
   **새 관측으로** 받으면 대기를 해제한다. 이미 지난 리셋 시각을 가진 늦은 입력은 무시한다.
   리셋 시각이 더 이른 창으로 되돌아가지 않으며 사용률 감소만으로 새 창을 판정하지 않는다.
4. 처음부터 해당 창을 제공받은 적이 없으면 성공한 입력의 누락을 `미제공`으로 표시한다.
   최초 입력 전은 `수집 중`이다. 리셋 전 일시적인 누락은 마지막 값을 과거 값으로 표시하고
   해당 창의 마지막 유효 수신 시각을 유지한다.
5. 나머지 유효 값은 출처별 신선도 기준으로 정상/오래됨을 판정한다. `원본 시각 불명`과
   `마지막 수신`은 상태와 별도로 표시한다. 한 창의 갱신이 다른 창의 신선도를 갱신하지 않는다.

사용률의 음수·비수치·잘못된 단위는 오류로 취급한다. 온도의 유효 범위는 해당 센서의 계약으로
판단한다. 100 초과 사용량은 원본과 진단에 보존하되 남은 바는 0으로 제한한다.
잔여 시간은 epoch seconds로 계산하고 0 아래로 표시하지 않는다.
절전 복귀·시계 변경 때 재계산한다. 표시 갱신은 원본의 관측 시간을 바꾸지 않는다.

### Claude

설정 UI의 `Claude 연결`은 `CLAUDE_CONFIG_DIR`을 데몬 전용 사용자 데이터 경로로 지정한
`claude auth login`을 시작한다. 공식 Claude Code가 그 경로의 자격 증명을 관리하며 제품은
`.credentials.json`을 직접 읽지 않는다. `claude auth status`는 연결 상태 확인에만 사용한다.
[공식 인증 문서][claude-auth]

statusline stdin의 `rate_limits.five_hour`와 `seven_day`에서 `used_percentage`, `resets_at`을
읽는다. 창은 각각 없을 수 있고 첫 API 응답 전에는 없을 수 있으며 계약·버전별 제공 조건이
다르다. 공식 문서상 리셋 시각이 지난 창은 입력에서 제거되므로, 이 누락에는 위의 `갱신 대기`
규칙을 적용한다. 설치된 버전의 실제 입력으로 지원 여부를 확인한다. [공식 statusline 문서][claude]

훅은 기존 statusline 출력을 보존하는 어댑터로 설치하고 패널이 꺼져 있어도 짧은 시간 안에
끝나야 한다. stdin 전체·대화·경로 정보를 저장하지 않는다. 세션별 파일로 동시 쓰기 충돌을 피한다.
사용량 원본 시각이 없으면 `source_observed_at=null`로 두고 **“마지막 수신”**이라고 표시한다.
동일 값의 반복 렌더를 새 API 관측으로 간주하지 않는다. 다른 세션의 뒤늦은 캐시가 더 최신이라고
확정할 수도 없으므로 선택 근거는 “최근 수신한 로컬 스냅샷”으로 한정한다.

이 경로는 집 Mac에서만 발생한 사용량을 실시간 보장하지 못한다. 오래된 값은 소진·리셋 여부에
따라 실제보다 높거나 낮을 수 있다. Mac 중계는 v2이며 v1은 출처와 신선도 한계를 드러낸다.

공식 문서에서 구조화된 5시간·7일 값은 활성 Claude Code 세션의 statusline 입력으로 제공된다.
로그인만 유지한 유휴 프로세스가 이 값을 임의 시점에 조회하는 공개 API는 현재 확인하지 못했다.
따라서 v1은 전용 프로필에서 Claude Code 활동이 있을 때 값을 갱신하고, 그 외에는 마지막 수신 시각과
`오래됨`을 표시한다. 내부 OAuth 엔드포인트 호출, 인증 파일 파싱, `/usage` TUI 화면 스크래핑은 사용하지 않는다.

### Codex

**선택: 데몬 전용 프로필의 Codex App Server `account/rateLimits/read`.** 설정 UI의 `Codex 연결`은
`account/login/start`의 ChatGPT 브라우저 흐름을 시작하고 완료 알림을 반영한다. 데몬이 stdio 자식
프로세스를 시작하고 `initialize`/`initialized` 후 조회한다. 대화나 모델 호출은 만들지 않는다.
Codex가 로그인과 자격 증명을 관리하며 이 프로젝트는 인증 파일·자격 증명 저장소를 직접 읽지 않는다.
기존 Windows 앱/CLI가 기본 `%USERPROFILE%\.codex`를 공유하더라도 데몬은 그 홈을 사용하지 않는다.
설치 PC에서 호환 버전·전용 홈 격리·무대화 조회·상주 비용을 G0에서 확인한다. [공식 계약][codex]

- 사용량은 최초 조회 후 30초마다 조회한다. 계정 변경·`account/rateLimits/updated` 알림은 즉시
  새 조회를 시작한다. 30초는 공급자가 정한 제한이 아니라 제품의 원본 조회 주기다. 완료된 최신
  스냅샷은 다음 1초 합성 틱에서 반영하며 화면 재합성만으로 이를 새 원본 관측으로 기록하지 않는다.
  실패·429에서는 재시도 간격을 늘리고 인증 실패는 재로그인 필요 상태로 둔다.
- `rateLimitsByLimitId`가 있으면 선택한 버킷(기본 후보 `codex`)을 읽고, 없으면 단일
  `rateLimits`를 사용한다. 예상 버킷이 없을 때 다른 버킷을 조용히 대신 쓰지 않는다.
- `usedPercent`, `windowDurationMins`, `resetsAt`을 정규화한다. `primary=5h`, `secondary=7d`로
  고정하지 않고 300분·10080분인지 검사한다. 다른 길이는 실제 길이를 표시하거나 미지원으로 둔다.
- 조회 실패를 로그로 조용히 덮지 않는다. **JSONL 수집은 사용자가 선택하는 대체 모드**다.
  선택한 홈의 `token_count` 이벤트 중 해당 버킷·창을 가진 최신 유효 레코드를 사용한다.
  파일 이름·mtime만으로 고르지 않으며 null·부분 기록·파일 교체·동시 세션을 처리한다.
  최초 제한된 탐색 후 증분 읽기를 사용하고 2초마다 전체 세션 트리를 읽지 않는다.

공식 조회와 로컬 로그는 신선도가 다르다. 공식 조회가 성공해도 서비스의 반영 지연까지 없어지는
것은 아니다. 로그 포맷은 공개 API처럼 고정된 계약으로 보지 않는다.

### 시스템 센서

온도 표시는 CPU·GPU 외에 RAM을 포함한다. RAM 온도의 미지원이 확인된 경우에만 사용자가 식별한
메인보드 온도 센서로 대체한다. 일시적인 읽기 오류나 오래된 값을 미지원으로 오인해 출처를 바꾸지 않는다.
대체값에는 실제 센서 ID·출처와 `메인보드 온도` 라벨을 유지한다. 둘 다 없으면 미지원으로 표시하고,
임의의 메인보드 센서(예: VRM·칩셋)를 이름만 보고 자동 선택하지 않는다.

CPU 사용률과 RAM은 `gopsutil/v4`를 우선한다. CPU는 샘플 간 변화량, RAM은 호스트 전체 사용률로
정의하며 첫 샘플이 불충분하면 수집 중으로 표시한다. 디스크·네트워크·업타임은 v1에 추가하지 않는다.

| 플랫폼 | 온도·GPU 방침 |
|---|---|
| Windows | LHM의 CPU package 온도와 선택 GPU의 core load/온도를 우선 평가. ACPI 서멀존을 CPU 온도로 대체하지 않음 |
| macOS | M3 Pro에서 gopsutil 온도 21개가 관측됐다는 기록 유지. `PMU tdie*`를 근거 없이 CPU로 이름 붙이지 않음. GPU 부하도 별도 검증 전 미제공 |
| Linux | hwmon·GPU 공급자가 제공하는 센서를 식별해 선택. 권한·하드웨어별 부재 허용 |

**2026-09-11 사용자 요구: 배포물은 standalone 실행을 목표로 한다.** LHM GUI 앱의 별도 설치·
상주·웹서버 설정을 제품 사용 조건으로 요구하지 않는다. `LibreHardwareMonitorLib`를 사용하는
작은 Windows 센서 보조 프로그램을 동봉하고 Go 데몬이 시작·종료를 관리하는 방향으로 변경한다.
보조 프로그램은 .NET self-contained로 배포해 사용자 PC의 별도 .NET 설치에 의존하지 않는다.
사용자가 ZIP 안에 실행 파일과 필요한 구성요소를 동봉하는 폴더형 배포를 선택했다.
[LHM 라이브러리 통합 안내][lhm], [.NET 배포 방식][dotnet-deploy]

보조 프로그램은 센서 읽기만 수행한다. 센서 ID·하드웨어 ID·종류·숫자/null·읽기 결과를
로컬 IPC로 전달하며 이름 기반 자동 선택이나 지역화된 표시 문자열 파싱을 하지 않는다.
ID가 바뀌면 재선택한다. 첫 진단 IPC는 `--stdio` 자식의 stdin에 `sample` 한 줄을 쓰고 stdout에서
`protocol_version=1` JSON 한 줄을 받는 요청·응답 방식이다. stderr는 크기를 제한해 별도 수집한다.
응답 상한은 2MiB, 첫 초기화 포함 응답 제한은 10초, 후속 응답 제한은 1500ms다.
요청은 직렬 처리하고 취소·시간 초과·프로토콜 오류 시 자식을 정리한다. 진단 경로는 자동 재시작하지 않는다.
진단 CLI는 명시한 로컬 helper 경로를 사용하고 Windows에서 자식 콘솔 창을 띄우지 않는다.
PawnIO 미설치 또는 일반 사용자 권한이면 CPU·메인보드·메모리 저수준 접근을 보수적으로 생략하고
사유를 전달한다. 이는 센서 미지원 판정이 아니다. GPU 조회와 gopsutil CPU·RAM 수집은 유지한다.
관리자 센서 프로세스의 설치·인증된 IPC·자동 시작은 별도 구현이며 진단 CLI가 권한을 자동 상승하지 않는다.
웹서버를 제품 경로에 추가하지 않는다. LHM의 `Update()` 호출 시각과 실제 센서 원본 관측 시각을
구분하고, 원본 시각을 알 수 없으면 null로 둔다. 센서별 오류 격리와 RAM 대체 규칙은 유지한다.

라이브러리 동봉만으로 드라이버 설치와 관리자 권한 요구가 사라지지는 않는다. 확인한 LHM 0.9.6은
PawnIO 설치 상태를 확인하고 해당 커널 장치를 연다. 필요한 센서의 드라이버·권한 범위, 드라이버
설치·제거 절차와 배포 조건을 실제 PC에서 검증해야 한다. 진단 수집 중 드라이버를 자동 설치하지 않으며
메인 앱 전체의 상시 관리자 실행이나 OS 보안 기능 해제를 기본 조건으로 삼지 않는다.
보조 프로그램·Go 진단 연동과 일반 사용자 GPU 수집은 구현·검증했다.
드라이버 설치와 CPU·RAM/메인보드 온도, 관리자 센서 프로세스의 제품 통합은 아직 검증하지 않았다.
[LHM 0.9.6 PawnIO 구현][lhm-pawnio], [gopsutil Windows 센서 소스][gopsutil-win]

**사용자 승인된 동봉 설치 방향:** standalone ZIP에 공식 서명된 PawnIO 설치 프로그램을 포함하고,
최초 설정에서 사용자가 온도 센서 사용을 선택하면 관리자 승인(UAC) 후 동봉 설치 프로그램을 실행한다.
사용자에게 PawnIO 사이트 방문이나 별도 다운로드를 요구하지 않는다. PawnIO 소스를 축소·재빌드한
드라이버는 만들지 않는다. `scripts/fetch-pawnio.ps1`과 `scripts/publish-sensors.ps1 -IncludePawnIO`는
공식 2.2.0 설치 파일을 고정 SHA-256·Authenticode 서명으로 검증하고 센서 publish 폴더의
`drivers/PawnIO_setup.exe`에 동봉한다. 설치 실행과 최초 설정 흐름은 아직 구현하지 않았다.

- 빌드 단계에서 공식 배포본의 버전을 고정하고 다운로드 출처·SHA-256·서명자 정보를 기록한다.
  패키징 시 Authenticode 서명과 고정 해시를 검증하고, 실행 직전에도 동봉 파일을 검증한다.
  첫 실행 시 인터넷에서 최신 설치 프로그램을 가져오는 방식은 사용하지 않는다.
- 최초 설정은 설치 상태 확인 → 온도 센서 활성화 안내 → UAC → 설치 종료 코드 확인 →
  새 helper 프로세스로 드라이버·권한·센서 상태 재조회 순서다. UAC 취소·설치 실패·재부팅 필요를
  구분하며, 설치 프로그램 종료 코드 0만으로 온도 수집 성공을 표시하지 않는다.
- 설치를 생략하거나 취소해도 CPU·RAM 사용률과 이용 가능한 GPU 센서를 제공한다.
  PawnIO 설치 자체로 일반 사용자 helper에 관리자 권한이 생기지는 않는다. 제품용 관리자 센서
  프로세스와 인증된 IPC는 별도로 설계·검증하며 메인 앱 전체의 상시 권한 상승으로 대체하지 않는다.
- PawnIO는 다른 앱도 사용하는 공유 드라이버이므로 우리 앱 삭제 시 무조건 함께 제거하지 않는다.
  배포본의 라이선스·소스 제공 조건과 의존성 고지를 확인하고 ZIP에 필요한 자료를 포함한다.
- 구현 검증은 정상 파일·변조 파일·서명 실패·UAC 취소·설치 실패·재부팅 필요·기설치 상태를 포함한다.
  실제 설치와 관리자 센서 읽기, .NET/LHM 미설치 PC에서의 ZIP 실행은 별도 통합 검증으로 남긴다.

필수 `ponytail` 스킬은 사용자 범위 `C:\Users\youhyun\.codex\skills\ponytail\SKILL.md`에
설치해 이번 구현에 적용했다. 동봉 staging을 드라이버 설치나 최초 설정 구현 완료로 취급하지 않는다.

수집 실패는 항목별로 격리한다. 초기 신선도 기준은 센서의 마지막 성공 샘플에서 10초,
Codex 공식 조회의 마지막 성공 응답에서 180초다. 로그는 유효 이벤트 시각에서 5분을 기준으로 한다.
Claude는 해당 창이 유효하게 포함된 마지막 새 훅 수신에서 5분이 지나면 오래됨으로 표시하고,
재시작 시 과거 파일은 이 유예 없이 오래됨으로 둔다. 수신이 계속돼도 **원본 시각 불명**
표시를 유지한다. 내용 변경 시각도 API 관측 시각을 대신하지 않는다.
이 값들은 **운용 초깃값**이며 제공자의 갱신 보장이 아니다.

## 5. 렌더링과 실시간 스트림

장치에는 레이어가 없다. 모든 합성은 호스트에서 한다. **데이터를 다시 그리는 횟수와
ffmpeg에 프레임을 공급하는 횟수는 다르다.**

```text
수집기 → 하드웨어 1초/사용량 원본 조회 30초·원자 스냅샷 → 테마 오버레이(1초마다 다시 그림)
                                ↓ 캐시한 투명 PNG를 25fps로 반복 공급
기성 mp4 루프 → ffmpeg ← stdin image2pipe
                  ↓ 1920×462 합성 → 시계 90도 회전 → 462×1920
                  ↓ H264 Annex B → stdout
             청크 조립 → 직렬 USB 전송 → 패널
```

### 첫 검증에 사용할 후보

- ffmpeg 프로세스를 유지하며 배경은 파일 입력으로 반복한다. 영상은 오디오 없이 사용한다.
  배경 비율은 테마의 crop/contain 선택으로 맞추고 두 입력의 PTS 시작점을 정렬한다.
- 오버레이는 Go에서 straight alpha PNG로 만들어 캐시한다. 25fps 입력으로 같은 인코딩 바이트를
  반복 공급하고 내용은 최대 1초마다 교체한다. 전체 RGBA rawvideo를 매번 보내는 약 88.7MB/s의
  호스트 파이프 비용과 저빈도 입력의 framesync 대기를 피하려는 **검증 전 후보**다.
- `overlay`의 `repeatlast`는 살아 있지만 다음 프레임이 오지 않는 입력의 비차단 보장이 아니다.
  PNG 파일 덮어쓰기를 ffmpeg가 자동 재로딩한다고 가정하지 않으며 지표 갱신마다 프로세스를 재시작하지 않는다.
  stdin 공급 중단·PTS·알파 가장자리·긴 실행의 드리프트를 G1에서 확인한다. [FFmpeg 필터 문서][filters]
- 실시간 입력 페이스는 호스트의 PNG 공급으로 제한한다. 진단 파일 반복 시험에서 `-readrate 1`을
  배경에 추가하면 루프 경계의 청크 대기가 1.5초를 초과해 현재 검증 도구에서는 제외했다. 장치 역압은 추가적인
  흐름 제어다. 무제한 사전 인코딩으로 오래된 지표를 버퍼에 쌓지 않는다. [FFmpeg 입력 속도][ffmpeg]
- 호환성 비교 기준은 `libx264`. Windows 하드웨어 인코더는 NVENC/QSV/AMF 중 실제 PC에서
  통과한 것만 선택한다. 하드웨어 인코더 존재를 PNG나 기본 개발의 선행 조건으로 삼지 않는다.
- 출력은 MP4가 아닌 H264 Annex B, 우선 8bit `yuv420p`, 25fps, B-frame 없음, 짧은 GOP와
  재시작 시 SPS/PPS·IDR을 제공하는 설정을 후보로 둔다. 프로파일·레벨·GOP·VBV 최종 값은
  장치 검증 후 고정한다. 1.5Mbit/s는 시작점이며 평균 비트레이트 옵션만으로 피크를 보장하지 않는다.

프레임 공급·stdout 청크 읽기·stderr 배출은 서로 막지 않게 진행한다. 오류 로그는 크기를 제한하고
수집 실패가 인코더 입출력을 중단시키지 않는다. 취소 시 모든 파이프와 자식 프로세스를 정리한다.

### 버퍼와 전환

지표는 최신 스냅샷 하나만 유지하고 USB 대기 청크도 우선 한 개로 제한한다. ffmpeg와 장치 내부
버퍼는 별도 측정 대상이다. 202752바이트를 1.5Mbit/s로 채우는 데만 약 **1.08초**가 걸리므로
청크를 채워 보내는 방식은 화면 지연에 직접 영향을 준다. 가령 실제 출력이 100kbit/s인 구간이면
같은 크기를 채우는 데 약 **16.2초**가 걸린다. 이는 계산 예시이며 장치 실측이 아니다.
평균 비트레이트 설정은 저변화 구간의 최소 출력량을 보장하지 않는다.

청크별로 **조립 대기 시작부터 USB 전송 시작까지**의 시간을 단조 시계로 잰다. 첫 바이트를
기다리는 시간·청크를 채우는 시간·완성 청크의 큐 대기를 모두 포함한다. 새 조립 작업을 시작할
때 타이머를 시작하며 추가 바이트가 올 때마다 초기화하지 않는다. `chunk_wait_limit`은 우선
**1500ms를 검증용 상한**으로 둔다. 읽기와 별도로 동작하는 타이머/취소 경로를 두어 stdout이
한 바이트도 주지 않거나 USB가 정체해도 상한 초과를 판정한다. 최종 값은 G1에서 정한다.

작은 청크의 허용 길이·경계·종료 플래그를 G1에서 확인한 경우에만 검증된 규칙으로 짧게 보낸다.
미확인 상태에서 시간만 보고 임의 분할하거나 패딩 바이트를 붙이지 않는다. 고정 크기만 가능하면
저변화 구간에서도 상한 안에 청크를 공급하는 인코더 설정을 장치에서 확인한다. 단순히 평균
비트레이트 수치만 높여 통과로 판단하지 않는다.

압축 스트림 중간의 바이트·P-frame을 버려 지연을 줄이지 않는다. 정체 시 생산을 멈추고,
대기 상한을 넘으면 스트림 전체를 중단한다. 전송 중인 트랜잭션은 자체 I/O 제한시간 내에서
종료·실패 처리한 뒤 정지 응답 처리·drain·sync를 수행한다. 폐기한 스트림의 미전송 바이트는
재사용하지 않는다. USB가 정상일 때 PNG로 전환하고, 영상 재시도는 초깃값으로 1초·2초·4초
간격의 최대 3회를 허용한다. 이를 소진하면 PNG를 유지하고 명시적 재시도나 테마 변경을 기다린다.
재시도 횟수는 영상이 30분 연속 정상 동작하거나 사용자가 명시적으로 재시도할 때 초기화한다.
영상 재시도는 새 SPS/PPS·IDR부터 시작한다. 단순 PNG는 미전송 프레임을 최신 것으로 교체할 수 있다.

청크 대기 상한은 호스트 정체를 감지하는 규칙이며 실제 패널 표시 지연의 측정값이 아니다.
오버레이 대기·인코더·USB·장치 버퍼까지 포함한 §8의 p95/최대 지연을 G1에서 별도로 측정한다.
상한을 지켰어도 표시 지연을 넘으면 G1 실패다. 재시작을 반복해 지연을 감추거나, PNG 복구만
성공한 실행을 연속 영상 검증 통과로 처리하지 않는다.

PNG 폴백은 **단순한 배경에 같은 지표·오류 상태**를 1초마다 갱신한다. 화려한 영상 프레임을
그대로 PNG로 바꾸지 않는다. 영상↔PNG·테마 교체는 송신 중인 트랜잭션을 마친 뒤 스트림 정지와
응답 처리를 거쳐 수행한다. 일시정지는 영상 정지 + 상태 표시를 뜻하며 USB 경쟁을 만들지 않는다.

## 6. 장치 전송 계약과 복구

현재 패킷 형식의 원본은 `internal/turzx/packet.go`다. 500바이트 평문을 DES-CBC/zero padding으로
504바이트 암호화하고 512바이트 프레임에 넣는다. 헤더 시각은 LE, 페이로드 크기는 BE다.
이 고정 키 암호화는 장치 호환 형식이며 인증·접근 제어로 보지 않는다.

### 영상 참조 구현에서 대조할 것

업스트림 3.10.0 `send_video`는 초기화 명령들(111/112/13/14/41 및 화면 지우기),
프레임레이트 명령 15, 청크 크기 조회 17을 사용한다. 협상이 유효하지 않으면 202752바이트를
기본으로 쓰고 명령 121 헤더 `[12]=1`로 파일의 마지막 청크를 표시한다. 122 응답으로
큐 상태를 확인하고 123으로 정지한다. [참조 소스][upstream]

이 순서의 각 명령 필요성, 122 응답 `[8]`의 정확한 의미, 파일 종료 플래그와 종료 없는 실시간
스트림의 관계는 **미확정**이다. 현재 Go 헤더 테스트에 포함되지 않는다. G1에서 최소 유효
초기화·응답 형식·청크 길이·종료 규칙을 확인하고 기준 패킷을 추가한 뒤 제품 드라이버로 옮긴다.

최소 sync/PNG/H264 검증 도구는 `gousb v1.1.3`을 사용한다. 보유 장치의 Windows 응답은
512바이트이며 sync는 `[0]=10, [1]=0xC8`, PNG는 `[0]=102, [1]=0xC8`이었다.
이 두 명령은 응답 명령 ID와 성공 마커를 확인하며 다른 명령의 응답을 성공으로 채택하지 않는다.
H264는 업스트림 순서와 패킷 배치, 협상값·큐 대기·정지 처리를 가짜 USB로 검사했다.
Windows에서 17/121/122/123의 명령 ID와 C8 응답을 확인했다. 큐 값의 정확한 의미와 실제 패널 동작,
다른 펌웨어의 호환성은 아직 확인하지 않았다.

### 전송 불변 조건

1. USB 핸들·IN/OUT·밝기·영상·PNG는 한 소유자가 직렬 처리한다. 벤더 앱과 동시에 점유하지 않는다.
2. 시작·복구 때 IN 큐를 **횟수와 총시간 상한 안에서** 비우고 sync 응답을 확인한다. drain 상한에
   도달하면 성공으로 진행하지 않는다. 정상 타임아웃과 장치 분리·권한·기타 I/O 오류를 구분한다.
3. write의 실제 길이와 오류를 확인하고 응답을 읽어 길이·알려진 필드를 검증한 뒤 bounded flush를
   수행한다. 부분 전송·응답 유실 때 중복 수신 여부가 불명확하므로 같은 청크를 무작정 재전송하지 않는다.
4. **인코딩된 PNG**의 크기 462×1920, bit depth 8, IHDR color type **6(RGBA)**, 허용 길이를
   확인한다. Go의 불투명 RGBA/NRGBA는 기본 인코더에서 RGB로 줄어든다. RGBA 출력을 강제하는
   작은 인코더 어댑터와 IHDR 검사로 보증한다. 픽셀 알파를 손상시키는 우회는 쓰지 않는다.
   팔레트/RGB 거부와 완전 불투명 화면을 테스트한다. [Go PNG writer][go-png]
5. 타임아웃·flush·재연결 간격은 장치 설정으로 한정한다. macOS 3ms를 전 OS에 고정하지 않는다.

기존에는 응답 읽기를 생략한 뒤 24프레임째 OUT이 막히고 stale 응답 110개가 관측됐다.
그 위험은 유지하되 이 숫자를 보편적 임계값으로 취급하지 않는다.

### 생명주기

`미연결 → 연결·drain·sync → 정적 표시 또는 영상 → 오류 시 정리·재연결`을 기본 흐름으로 둔다.
인코더 오류는 USB가 정상일 때 PNG로 내려가고 USB 분리는 미리보기·지표 수집을 유지한다.
재연결은 1초부터 최대 30초까지 간격을 늘리는 초깃값을 사용한다. 재연결·절전 복귀 후에는 이전
압축 청크를 재사용하지 않고 새 스트림을 시작한다. 제한된 재동기화로 회복되지 않으면 트레이에
케이블 재연결 필요를 표시한다. 종료는 스트림 정지 시도에 제한시간을 두고 자식 프로세스와 핸들을 닫는다.

## 7. 라이브러리 선택·구조·배포

### Go 관례를 구현 기준으로 삼는다

Go에는 모든 프로젝트가 따라야 하는 단일 폴더 표준이 없다. 이 프로젝트는 공식
[모듈 구조 가이드][go-layout]와 [Go Code Review Comments][go-review]를 기준으로 삼는다.

- 단일 Go 모듈, 실행 진입점은 `cmd/turzx-control`, 비공개 구현은 `internal/`에 둔다.
  책임 경계는 실제 코드가 생길 때 패키지로 나눈다. 빈 계층이나 `utils`/`common` 만능 패키지를 만들지 않는다.
- 짧고 역할이 분명한 패키지명, 최소한의 exported API, 생성자에는 구체 타입을 우선한다.
  인터페이스는 필요가 드러난 소비자 쪽에 작게 정의한다. DI 프레임워크·클래스 계층을 옮기지 않는다.
- 예상 가능한 실패는 `error`로 반환하고 `%w`로 문맥을 추가한다. 분기는 `errors.Is/As`를 사용하고
  오류는 최종 처리 경계에서 한 번 기록한다. 장치 분리·입력 오류를 `panic`으로 처리하지 않는다.
- 취소 가능한 I/O·장기 작업은 `context.Context`를 첫 인자로 받고 요청 컨텍스트를 설정 저장소로 쓰지 않는다.
  goroutine마다 시작 주체·종료 조건을 정한다. 데이터 공유에는 mutex, 작업 전달에는 channel을 목적에 맞게 쓴다.
- `io.Reader/Writer`, `net/http`, `os/exec`, `log/slog`, `encoding/json`, `embed` 등 표준 추상화를 활용한다.
  작은 장치 어댑터 때문에 라이브러리와 유사한 자체 프레임워크를 만들지 않는다.
- `*_windows.go`/`*_darwin.go`/`*_linux.go`와 필요한 build constraint로 OS 경계를 둔다.
  트레이 라이브러리가 요구하는 메인 스레드 제약을 진입점에서 지킨다.
- 형식은 `gofmt`, 기본 검사는 `go vet ./...`와 `go test ./...`다. 상태 전이·데이터 파싱·패킷·
  취소 처리는 표준 `testing`으로 검사하고 동시성 경로는 지원 플랫폼에서 race detector를 적용한다.
  테스트용 만능 mock 계층을 만들지 않고 외부 I/O 경계에 작은 대역을 둔다.

### 선택 원칙과 권장 조합

**기능별로 적합한 공개 구현을 조합한다.** 제조사/참조 앱의 테마·센서·설정 시스템을 통째로
포팅하지 않는다. 장치 프로토콜도 패킷 포맷뿐 아니라 응답·버퍼·복구까지 가져와 검증해야 한다.
라이브러리 선택은 실제 Windows/macOS/Linux 지원, 유지 상태, 필요한 기능, 배포 비용으로 판단한다.
아래는 채택 방향이며 의존성을 설치했다는 뜻은 아니다. 버전은 구현 검증 시 고정한다.

| 역할 | 권장 선택 | 프로젝트가 담당할 부분 |
|---|---|---|
| CPU·RAM·일반 시스템 지표 | [`gopsutil/v4`][gopsutil] | 지표 이름·단위·샘플 주기·누락 상태 |
| Windows CPU/GPU 온도·GPU 부하 | [LibreHardwareMonitorLib][lhm] 기반 동봉 보조 프로그램 | 로컬 IPC→지표 변환, 센서 선택, 상태·권한·자식 관리 |
| macOS/Linux 온도 | gopsutil의 제공 센서 우선 | 실제 센서 의미 확인. 없으면 미제공 |
| USB 호스트 접근 | [`google/gousb`][gousb] / libusb | TURZX 패킷·응답·직렬 전송·복구 |
| 영상 디코딩·합성·회전·인코딩 | [FFmpeg][ffmpeg] 외부 프로세스 | 안전한 인자 구성·파이프·페이싱·생명주기 |
| 글꼴·이미지·막대 오버레이 | Go `image`/`image/draw` + [`x/image/font/opentype`][font] | 테마 레이아웃·지표 서식. 폰트 래스터라이저를 직접 만들지 않음 |
| 곡선·복잡한 2D 도형이 필요한 디자인 | [`fogleman/gg`][gg]를 추가 평가 | 실제 시안에 필요할 때만 채택. 자체 도형 엔진을 만들지 않음 |
| 트레이 | [`getlantern/systray`][systray] | 메뉴·OS 생명주기 연결 |
| 설정·테마 파싱·저장 | `net/http`, `encoding/json`, `os` | 제품 설정 검증·원자 저장 |

**센서를 전부 감추는 단일 Go 라이브러리로 고정하지 않는다.** gopsutil은 CPU/RAM에는 적합하지만
Windows 온도 구현은 ACPI 서멀존이며 CPU package/GPU 센서 범위를 대신하지 못한다.
[`ghw`][ghw]는 정적 하드웨어 사양·능력 조사용으로 실시간 모니터링 라이브러리가 아니다.
NVIDIA 공식 [`go-nvml`][nvml] 바인딩은 현재 Linux만 지원하므로 Windows의 공통 GPU 어댑터로
선택하지 않는다. Linux NVIDIA 지원이 필요하거나 LHM으로 필요한 GPU 값을 얻지 못할 때만
해당 플랫폼에 맞는 공급자 도구를 추가한다. [Windows 온도 구현][gopsutil-win]

LHM 라이브러리를 재사용해 센서 접근·하드웨어 지원을 직접 유지하지 않는다. 동봉 센서 프로세스는
우리 앱이 관리하며 사용자가 LHM GUI를 직접 실행하지 않는다. v1에서 센서 드라이버나 Go↔CLR
브리지를 직접 구현하지 않는다. 보조 프로그램과 런타임·드라이버의 비용도 배포 검증에 포함한다.

### 책임 경계

```text
cmd → ui → daemon
             ├→ metric : 수집·정규화된 최신 스냅샷
             ├→ theme  : 파일 검증·배경/배치/지표 바인딩
             ├→ render : 오버레이·합성·인코딩·정적 미리보기
             └→ turzx  : 패킷·USB 트랜잭션·장치 상태
```

`daemon`이 결과를 전달하고 생명주기를 조정한다. `render`는 USB를 직접 열지 않고,
`theme`는 수집기를 호출하지 않으며 `turzx`는 지표를 모른다. 작은 구조체·함수로 시작한다.
범용 이벤트 버스·동적 플러그인·한 구현만을 위한 인터페이스 계층은 만들지 않는다.
지표는 고정 ID 스냅샷으로 조회하고 OS별 수집 구현만 빌드 태그로 나눈다.

### 테마와 설정

테마 포맷은 v1 JSON으로 시작한다. `version`, 이름, 배경 종류·로컬 소재 경로·맞춤 방식,
텍스트/막대 위젯의 지표 ID·위치·크기·서식·색·폰트 참조만 지원한다. 실제 배치는 디자인
마일스톤에서 정한다. 테마는 ffmpeg 명령·인코더 이름·실행 코드·네트워크 URL을 담지 않는다.
네이티브 해상도·회전·인코더·USB 튜닝은 장치/데몬 설정의 책임이다.

테마 로딩은 버전·좌표·지표 ID·소재·폰트 존재와 경로 범위를 검증한다. 알 수 없는 지표는 오타로
보고 적용을 거부하고 지원하지 않는 센서의 알려진 ID는 `—`로 렌더한다. 잘못된 새 테마는 현재
테마를 유지한다. 소재는 테마 폴더 안에서 참조하며 실행 인자는 shell 문자열로 조립하지 않는다.

트레이는 열기·일시정지·밝기·종료, 설정 화면은 출처/센서 선택·테마 선택/재로드·오류 진단을 맡는다.
미리보기는 같은 스냅샷·레이아웃·폰트를 쓴 **정적 합성 PNG**다. 장치 H264 바이트를 브라우저에
그대로 재생하거나 실제 패널의 시간·압축 화질까지 동일하다고 약속하지 않는다.

로컬 설정 서버는 `127.0.0.1`에만 바인딩한다. 변경 요청은 POST와 Origin/Host·세션 토큰을
검증하며 광범위 CORS를 열지 않는다. 설정은 사용자 전용 경로에 원자 저장하고 단일 인스턴스만
실행한다. 트레이 데몬에 딸린 작은 설정창은 `net/http` HTML을 사용한다. 기존 프로젝트의
`@bonggu/ui` 예외는 유지하며 제품 표면으로 커지면 다시 검토한다.

### 설치 조건

Go 선택은 유지한다. standalone 배포물에는 libusb·FFmpeg와 Windows 센서 보조 프로그램 및
필요 런타임을 동봉하는 방향이며, 사용자의 개발 도구 설치 경로를 참조하지 않는다.
센서·USB 드라이버와 AI 공급자 로그인은 별도 조건이다. Codex·Claude 공식 실행 파일의 동봉 가능 여부와
업데이트 정책은 아직 결정하지 않았다. 없으면 설정 UI가 설치 필요 상태와 공식 설치 경로를 안내한다.
OS별 빌드·패키징을 검증한다.

- Windows: 현재 USB 드라이버와 libusb에서 사용할 수 있는 WinUSB 계열 바인딩을 먼저 확인한다.
  변경이 필요하면 대상 VID/PID와 벤더 앱 복구 방법을 설치 절차로 준비한다. 드라이버를 자동 교체하지
  않는다. 일반 사용자 데몬과 관리자 센서 프로세스를 구분한다. [libusb Windows 문서][libusb-win]
- macOS/Linux: libusb 로딩·USB 권한·트레이 환경을 각각 확인한다. 트레이가 없는 환경에서도
  헤드리스 상태 확인이 가능해야 한다.
- ffmpeg: 초기 검증은 지정된 외부 실행 파일을 사용한다. 배포 전 OS별 검증 빌드·필수
  필터/인코더·버전을 고정한다. ffmpeg가 없거나 실패해도 Go PNG 폴백은 동작해야 한다.
- 글꼴·기본 정적 자산은 재배포 가능한 파일로 묶어 설치 폰트에 의존하지 않는다.
  상용 모션그래픽은 사용자가 제공하는 로컬 파일로 시작한다.
- 기존 GPL-3.0-or-later·SPDX·업스트림 출처 고지를 유지한다. 실제 배포하는 libusb·ffmpeg 빌드·폰트·
  영상의 구성과 고지는 배포물 기준으로 확인하며 현재 프로젝트 라이선스로 일괄 대체하지 않는다.

### 빌드·서명·배포·실행

**컴파일 성공과 다른 PC에서 신뢰받는 실행은 별도 완료 조건이다.** 개인 개발 실행, 개인용 시험
배포, 서명된 정식 배포를 구분한다. 인증서 구매·계정 등록·서명·릴리스 발행은 이번 문서 작업에서
수행하지 않는다. 인증서 보유 여부·개인/조직 명의·비용은 배포 준비 단계에서 결정할 외부 조건이다.

| 환경 | 개발·시험 | 정식 배포 계획 |
|---|---|---|
| Windows | 네이티브 빌드와 실제 다운로드한 ZIP을 각각 시험. 미서명본은 개발용으로 명시 | 실행 파일·필요한 DLL/보조 실행 파일의 서명 범위 확인, Authenticode 서명과 타임스탬프. 초기 형태는 버전별 portable ZIP |
| macOS | 로컬 CLI 빌드와 메뉴바 `.app` 번들을 모두 시험. 로컬/ad hoc 서명은 개발용 | Developer ID Application 서명 + Hardened Runtime + 공증, ticket을 붙인 `.app`을 DMG로 배포 |
| Linux | 배포판별 네이티브 의존성·USB 권한·최소 glibc 환경 시험 | 바이너리·자산·의존성 안내를 tar.gz로 제공. 지원 배포판/아키텍처 명시, 체크섬 제공 |

**Windows.** Authenticode는 게시자와 무결성을 확인하는 수단이고 SmartScreen은 평판도 본다.
새 서명 바이너리에도 경고가 나올 수 있으며 EV 인증서도 즉시 경고 제거를 보장하지 않는다.
Smart App Control이나 회사 정책은 다운로드 여부와 별개로 실행을 차단할 수 있다.
ZIP·체크섬·GitHub 릴리스만으로 이런 신뢰가 생기지는 않는다. [Microsoft 설명][smartscreen]

공개 배포는 발급 가능한 CA 코드 서명 인증서 또는 Microsoft Artifact Signing을 검토한다.
2026-09-10 확인한 공식 안내에서 Artifact Signing의 **개인 개발자 Public Trust는 미국·캐나다로
제한**된다. 한국 거주 개인이라면 이 서비스를 전제로 설계하지 않고 실제 발급 가능한 CA의 조건을
먼저 확인한다. 자체 서명 인증서는 일반 사용자 PC의 공개 신뢰를 대신하지 못한다.
[서명 선택지][win-signing], [서비스 자격 조건][artifact-signing]

서명은 최종 PE 리소스·버전 정보를 넣은 뒤 수행한다. 배포하는 보조 실행 파일과 라이브러리도
서명·차단 여부를 검사하며, 사용자 모드 앱 서명이 WinUSB 설치나 LHM의 커널 드라이버 신뢰까지
해결한다고 가정하지 않는다. 검증에는 [SignTool][signtool]의 서명·타임스탬프 검사와 실제 다운로드 후
표준 사용자 실행을 포함한다. 앱 전체 관리자 실행이나 보안 기능 끄기를 기본 설치 절차로 삼지 않는다.

**macOS.** Go 앱뿐 아니라 동봉한 ffmpeg·libusb 등 실행 코드도 서명 계획에 포함한다.
Developer ID를 발급할 수 있는 Apple Developer Program 계정과 서명 자격 증명을 배포 조건으로 둔다.
개발 머신의 Homebrew 경로를 배포본이 참조하지 않게 로딩 경로를 정리한 뒤, 중첩 코드를 먼저 서명하고
최종 앱을 서명한다. Hardened Runtime과 필요한 최소 entitlement를 적용하고 `notarytool`로 공증한다.
실제 ffmpeg 자식 실행·dylib 로딩·USB 접근까지 확인한다. [Apple 공증 요건][apple-notary]

ZIP이나 단독 실행 파일에 ticket을 직접 staple할 수 없으므로 `.app`/DMG 구조를 택한다.
앱에 ticket을 붙이고 최종 DMG도 공증·staple·검증한다. 브라우저로 받은 파일의 quarantine이
유지된 깨끗한 Mac에서 Gatekeeper 검사와 첫 실행을 확인한다. 서명 후 번들 안에서 설정·테마를
고치지 않고 사용자 데이터 경로에 저장한다. [Apple 배포 워크플로][apple-workflow]

**릴리스 파이프라인.** 표준 Go 도구와 OS별 CI 작업부터 시작한다. cgo 의존성이 있으므로
`GOOS`만 바꾼 크로스컴파일로 전 플랫폼을 약속하지 않는다. 초기 대상은 Windows amd64 후보,
macOS arm64, Linux amd64 후보이며 G0에서 사무실 PC 아키텍처와 지원 OS 하한을 확정한다.

1. Go·C 툴체인·libusb·ffmpeg와 Go 모듈 버전을 고정하고 같은 소스 태그에서 빌드한다.
   `go.mod`/`go.sum`을 관리하고 버전·커밋·OS/arch를 바이너리 진단에 포함한다.
2. 형식·vet·단위 검사와 플랫폼별 빌드 후 패키지 의존성 누락을 검사한다. USB 실물 검사는 CI의
   대체물로 보지 않고 해당 OS 장치에서 별도로 수행한다.
3. 자산·라이선스 고지·런타임을 배치하고 최종 코드 서명→공증/검증→아카이브 순서로 마무리한다.
   최종 배포 파일의 SHA-256을 생성한다. 서명 타임스탬프 등으로 파일 해시가 달라질 수 있으므로
   소스/툴체인 추적 가능성과 서명된 파일의 바이트 재현성을 혼동하지 않는다.
4. 서명 키·공증 자격 증명은 저장소에 넣지 않고 보호된 CI 자격 증명으로만 전달한다.
   PR 빌드와 릴리스 서명 작업을 나누고 서명 실패 시 미서명본을 정식 버전으로 대신 게시하지 않는다.
5. 개발 도구가 없는 깨끗한 사용자 환경에서 다운로드→검사→설치/압축 해제→실행→업데이트→제거를
   시험한 뒤 릴리스한다. 지원 범위·필수 외부 프로그램·개발용/서명 여부를 릴리스에 표시한다.

v1 업데이트는 종료 후 버전별 배포물을 교체하는 수동 방식이다. 설정·테마는 바이너리 외부 사용자
경로에 유지하고, 설정 버전 변경 전 백업으로 이전 버전 복구를 가능하게 한다. 자동 시작은 사용자
로그인 시 트레이 앱 실행으로 두고 Windows 서비스 세션에 UI를 띄우지 않는다. 등록과 제거는
대칭으로 제공한다. 자동 업데이트·Store/MSIX 배포는 실제 배포 부담이 생길 때 별도 검토한다.

## 8. 검증 관문과 구현 착수 순서

**G0/G1 검증 도구부터 구현한다.** G0/G1을 먼저
수행하고 결과로 스트림·수집 계약을 확정한다. Windows 확인이 장치 없는 파서 검토나 시각 디자인을
막을 필요는 없지만 완성형 테마 엔진·설정 UI에 앞서 핵심 경로가 성립해야 한다.

**2026-09-11 사용자 결정:** 30분 연속 검증을 구현 진행의 필수 선행 조건에서 제외하고,
장시간 안정성은 실제 사용 중 후속 검증한다. 아래 G1 목표는 검증 기준으로 남기지만 미통과를
이유로 수집기·테마 구현을 중단하지 않는다. 장시간 안정성·표시 지연·분리 복구를 검증 완료로 표기하지 않는다.

| 단계 | 할 일 | 통과 조건 |
|---|---|---|
| G0: 운용 환경 | 사무실 Windows USB 바인딩·일반 사용자 접근, CPU/GPU 센서, Windows/WSL inbox, Codex 무대화 조회·계정 대조 | 장치·센서 ID·계정·소스 경로·필수 실행 파일과 수동 재연결 조건 결정. 동시 수신과 WSL 종료가 호스트 수집을 막지 않음 |
| G1: 기술 검증 | 최소 USB 전송 + 계속 바뀌는 시각/카운터 오버레이 + 실제 모션 배경. 121 종료 규칙·122·청크·실시간 공급·대기 상한 | 저변화 구간과 반복 경계를 포함한 30분 연속 영상, 인코더 재시작·지연 누적 없음. 정체 주입 시 제한된 복구 확인. 아래 지연/복구 목표 측정 |
| G2: 데이터 | 작은 수집기·정규화·출처/상태 정책·연결 세대·inbox 재시작 규칙 구현 | null·0·리셋 후 누락·다중 버킷·계정 전환·과거/중복 파일·동시 세션·부분 JSONL·센서 오류를 구분하는 검사 통과 |
| G3: 첫 테마 | 별도 디자인 마일스톤의 시안으로 JSON 테마·글꼴·PNG/H264 경로 완성 | 영상의 밝고 복잡한 구간에서도 모든 요구 지표와 오래됨 상태를 실제 패널에서 읽을 수 있음 |
| G4: 일상 운용 | 트레이·설정·자동 시작·OS별 패키징·장시간 실행 | 사무실 PC 8시간 운용, 종료 시 자식 정리, 재부팅/절전/분리 복구, 아래 자원 목표 확인 |
| G5: 배포 검증 | 서명 자격/비용 결정, OS별 서명·공증·최종 패키징 | 개발 도구 없는 환경에서 실제 다운로드본 실행·업데이트·제거 확인. 서명/공증 검사 통과. 미서명본은 개발용으로 구분 |

현재 Windows 검증 환경은 Windows 11 Pro 10.0.26200 amd64, i7-12700KF, RTX 5060,
WINUSB 바인딩, Go 1.27.1, MSYS2 UCRT64 GCC 16.2.0, libusb 1.0.30,
FFmpeg 9.0.1 essentials다. 비관리자 프로세스에서 `go run ./cmd/turzx-probe -test-pattern`의
sync와 PNG 응답을 확인했다. 사용자는 빨강·초록·파랑·흰색 네 색상 띠가 화면 전체에
정상 표시됨을 확인했다. timeout 2초·flush 20ms에서 각각 약 74ms·75ms의 단일 관측이며
실제 표시 지연·통계·최적 flush 값으로 해석하지 않는다. `libx264`와 `h264_nvenc`는
462×1920 단일 프레임 인코딩에 성공했으나 해당 스트림의 패널 재생은 아직 확인하지 않았다.
G0의 센서·inbox·계정 검증과 G1의 실제 배경·연속 스트림·표시 지연 검증은 남아 있다.

### 현재 작업 재개 지점

**2026-09-11 현재 중단 지점:** 사용자 요청에 따라 아래 센서 연동 스텝을 완료하고 멈췄다.
새 지시 전에는 드라이버 설치·AI 사용량·오버레이 연결·장시간 실험을 이어서 진행하지 않는다.

- `internal/metric/hardware.go`와 테스트, USB를 열지 않는 `cmd/turzx-metrics`를 구현했다.
  `gopsutil/v4 v4.26.8` 채택과 `go mod tidy`를 완료했다. CPU는 수집기별 누적 시간 차이로
  계산하고 첫 표본은 `collecting`/null이다. 오류를 가짜 0으로 바꾸지 않는다.
  기존 CPU·RAM 수집 동작은 유지하고 센서 ID·라벨·로컬 수신 시각·오류 메타데이터를 추가했다.
- `tools/turzx-sensors`에 .NET 8 Windows 보조 프로그램을 구현했다. `LibreHardwareMonitorLib 0.9.6`과
  전이 의존성은 `packages.lock.json`에 고정했다. LHM GUI·웹서버는 필요 없다.
  기본 1초 JSON 출력, `--stdio` 요청·응답, `--self-test`를 제공한다.
- `internal/metric/sensor_helper*.go`에 자식 시작·직렬 요청·응답 크기 제한·기한·취소·종료를,
  `sensor_selection.go`에 센서별 정규화와 명시적 RAM→메인보드 대체를 구현했다.
  선택 ID 누락·중복·잘못된 종류·null·범위 오류는 해당 항목만 error/null이다.
  미선택 항목은 unconnected이며 RAM 미지원 확인은 다른 센서 수집을 막지 않는다.
  수신 시각은 `received_at`, 원본 시각을 모르는 LHM `observed_at`은 null이다.
  제품의 과거 값 캐시·10초 신선도 판정·렌더 연결은 아직 없다.
- 최종 Windows `go test ./...`, `go vet ./...`, `go build ./...`, `go test -race ./...` 모두 통과했다.
  race는 샌드박스 내 GCC 링크 실패 후 승인된 실행에서 통과했다. C# build/publish는 경고 0·오류 0,
  `--self-test` 숫자·null 검사와 publish 스크립트 구문 검사를 통과했다.
  Go 센서 패키지·진단 CLI는 `CGO_ENABLED=0`으로 Linux·macOS 크로스 빌드도 성공했다.
  해당 OS에서 실행한 것은 아니며 Windows C# 센서 helper의 타 OS 지원을 뜻하지 않는다.
  크로스 빌드 시 Go 모듈 stat 캐시 쓰기 권한 경고가 있었지만 두 빌드의 종료 코드는 0이었다.
  reviewer의 프로세스 채널 종료·deadline 경계 지적을 수정한 뒤 최종 검토에서 치명적 미해결 문제는 없었다.
- 실제 RTX 5060의 core load ID는 `/gpu-nvidia/0/load/0`, core temperature는
  `/gpu-nvidia/0/temperature/0`이다. 최종 Go CLI 3표본에서 GPU 4~5%, 약 33.6~33.7°C,
  CPU 첫 표본 collecting 후 약 7.7~29.4%, RAM 44%를 관측했다. 짧은 진단값이며 운용 성능 보장은 아니다.
  `driver_installed=false`, `elevated=false` 상태에서도 GPU 조회에 성공했다. 종료 후 helper 잔류 없음.
  원본 목록의 `/gpu-nvidia/0/load/3`은 Bus/Memory에 중복되어 있어 선택하지 않는다.
  미선택 ID 중복이 정상 core 수집을 막지 않는 검사도 추가했다.
- 개발용 SDK 8.0.425를 `.tools/dotnet`에 준비했다. 시스템 PATH를 변경하지 않았다.
  `scripts/publish-sensors.ps1`로 self-contained publish를 재현하며 기존 출력 폴더는 덮어쓰지 않는다.
  최종 검증본은 `artifacts/sensors-win-x64-verified/turzx-sensors.exe`와 같은 폴더의 DLL·런타임이다.
  `coreclr.dll`·`hostfxr.dll`, runtimeconfig의 includedFrameworks(.NET 8.0.31)를 확인했다.
  이 폴더는 센서 보조 프로그램만의 개발 산출물이며 전체 제품 ZIP이나 배포·서명 검증 완료본이 아니다.
  실제 실행 재현은 `go run ./cmd/turzx-metrics -sensor-helper artifacts/sensors-win-x64-verified/turzx-sensors.exe -gpu-usage-sensor /gpu-nvidia/0/load/0 -gpu-temperature-sensor /gpu-nvidia/0/temperature/0 -samples 3`이다.
- PawnIO Setup 2.2.0의 공식 release URL과 SHA-256
  `1f519a22e47187f70a1379a48ca604981c4fcf694f4e65b734aaa74a9fba3032`, Authenticode
  `CN=namazso.eu`를 고정 검증하는 `scripts/fetch-pawnio.ps1`을 추가했다. 센서 publish는
  `-IncludePawnIO`에서 검증된 파일을 `drivers/PawnIO_setup.exe`로 복사하고 `components.json`을 남긴다.
  동봉/미동봉 publish와 helper self-test, 변조 파일 거부를 확인했다. 설치 파일은 실행하지 않았으며
  최초 설정·UAC·설치 후 재조회는 아직 없다.
- **추가 설치 조건:** LHM 앱과 배포 PC의 .NET 런타임 별도 설치는 필요 없다.
  현재 PC에는 PawnIO가 확인되지 않았다. CPU·메인보드·메모리 저수준 온도 검증은
  [PawnIO 공식 배포 안내](https://github.com/namazso/PawnIO)의 드라이버 설치와 권한 확인이 다음 조건이다.
  이번에는 드라이버·서비스 설치와 자동 권한 상승을 수행하지 않았다. 설치했다고 센서 지원까지 보장하지 않는다.
  기존 Go·MSYS2/libusb·FFmpeg 개발 환경은 유지한다. AI 계정 연동 조건은 다음 단계에서 확인한다.
- 2026-09-11 재개 검증에서도 일반 사용자 권한(`elevated=false`)과 PawnIO 미설치
  (`driver_installed=false`)가 확인됐다. 같은 실행에서 RTX 5060 core load와 core temperature,
  CPU·RAM 사용률은 계속 정상 수집됐고 CPU·RAM/메인보드 온도만 미연결이었다. helper 종료 후
  잔류 프로세스는 없었다. self-contained 폴더는 199개 파일, 약 79.2 MB이며 `DOTNET_ROOT`를
  존재하지 않는 경로로 지정하고 `DOTNET_MULTILEVEL_LOOKUP=0`으로 실행한 `--self-test`도 종료 코드 0이었다.
- Codex App Server 실측을 시작했다. 공식 계약대로 대화나 모델 호출 없이
  `initialize`/`initialized` 후 `account/rateLimits/read`가 성공했다. Windows의 Codex CLI 0.154.0은
  `codex` 10080분 창 사용 25%(2026-09-18 08:36 KST 리셋)를 반환했다. 별도
  `GPT-5.3-Codex-Spark` 버킷은 300분 창 사용 100%(2026-09-11 16:46 KST 리셋)와 10080분 창
  사용 44%(2026-09-18 11:46 KST 리셋)를 반환했다. 이는 해당 시각의 진단 관측이며 고정 한도나
  이후 표시값이 아니다.
- WSL Rocky-9의 Codex CLI 0.153.4도 같은 조회에 성공했으나 Windows와 다른 Team 계정 범위였고,
  `codex` 10080분 창 사용 100%, `workspace_member_credits_depleted`, 2026-09-16 08:41 KST 리셋을
  반환했다. 이 값은 전용 프로필 결정 전의 진단 기록이며 v1 데몬은 해당 WSL 홈을 읽지 않는다.
  응답의 계정 ID는 문서나 로그에 기록하지 않는다.
- Windows와 WSL 모두 Claude Code 2.1.268이 설치되어 있으나 기존 사용자 설정에 `statusLine`이 없다.
  따라서 공식 `rate_limits.five_hour`·`seven_day` 입력 계약은 확인했지만 실제 사용량 수신은 아직
  연결되지 않았다. v1은 기존 프로필을 수정하지 않고 전용 `CLAUDE_CONFIG_DIR`에서 로그인·statusline
  어댑터·연결 세대·세션별 inbox를 구성한다. 유휴 상태의 구조화 한도 조회는 지원하지 않는다.
- 재개 순서: PawnIO 설치·권한 경계 결정 → CPU/RAM/메인보드 센서 실제 목록 확인·명시적 선택 →
  Claude·Codex 수집 → 오버레이 연결. 하드웨어·전체 합성 1초, 사용량 원본 조회 30초와
  원본 신선도 구분은 유지한다.
  첫 테마 코드 시안은 구현했으며 실제 패널 가독성·실데이터 연결, 제품 데몬, 전체 ZIP 패키징은 아직 미완료다.
- 사용자 요청으로 30분 실험을 중단하고 해당 probe·FFmpeg 정리를 확인했다.
  장시간 테스트를 자동 재개하거나 구현 선행 조건으로 되돌리지 않는다.
  `scripts/measure-stream.ps1`은 선택적 측정 도구이며 정리 보강 후 구문만 검사했다.
- 이번에는 `spark_explorer`·`spark_worker`와 `reviewer`를 실제 사용했다. Spark 사용량 한도에
  도달한 뒤 미완료 Go 연동·정규화 수정은 메인이 이어받아 최종 검증했다.
- 첫 테마 계약을 `assets/backgrounds/azure-ribbon.theme.json`과 `internal/theme` 로더로 구현했다.
  v1은 1920×462 canvas, 폴더 안의 mp4 배경, Pretendard 내장 참조, palette, region 상대 요소 좌표,
  알려진 지표 ID와 전체 합성 1000ms를 사용한다. 알 수 없는 필드·중복 JSON 키·중복 요소·경로 탈출·
  범위 밖 좌표를 거부한다. Windows symlink/junction 탈출은 이 PC의 생성 권한 부족으로 실검증하지 못했다.
  manifest 기반 실제 렌더러와 설정 웹 UI는 아직 미구현이다.
- `internal/render.AzurePreviewOverlay`와 probe의 `-theme azure-ribbon`을 추가했다. 화면은 사용자의
  수정에 따라 시계, `AI 에이전트`, `하드웨어 모니터`로 묶고, 값은 `미리보기 데이터`로 명시한다.
  기존 `-background` 진단 오버레이는 기본값으로 유지한다. 실제 FFmpeg 합성 PNG를 확인했고 Windows
  패널에 30초 전송해 27청크, 최대 큐 깊이 1, 정지 응답 C8과 프로세스 정리를 확인했다.
  실제 패널의 작은 글자 가독성은 사용자 확인 전까지 미검증이다.
  사용자 피드백으로 시계 전용 왼쪽 열을 만들고 날짜를 `YYYY-MM-DD`로 배치했으며 두 데이터 영역을
  오른쪽으로 옮겼다. 글자와 게이지를 키우고 Pretendard로 교체했으며 가능한 라벨을 한글화했다.
  Pretendard는 Regular/SemiBold OTF와 OFL 원문을 `internal/render/fonts/`에 고정 동봉한다.
  후속 조정에서 AI·하드웨어 영역의 좌우 안쪽 여백을 같은 기준으로 맞추고, 남는 폭은 시계 열에
  배정해 시각을 더 키웠다. 이 버전도 Windows 패널에 30초 전송해 27청크·정지 응답 C8을 확인했다.
  첫 패널 확인 의견에 따라 전체 글자 크기를 높이고 특히 리셋 시간·온도·날짜를 확대했으며,
  날짜 형식은 `YYYY-MM-DD`로 바꿨다. 수정본도 30초 전송해 27청크, 최대 큐 깊이 1,
  정지 응답 C8과 프로세스 정리를 확인했다. 수정본의 가독성은 사용자 확인을 기다린다.
  시계에는 초를 추가하고 테마 오버레이를 1초마다 다시 그리도록 변경했다. 최신 30초 패널 전송에서
  counter 0..29, 27청크·5,474,304바이트, 최대 큐 깊이 1, 최대 청크 대기 1198ms와 정지 응답 C8을
  확인했다. 최신 초 표시본의 실제 패널 가독성은 사용자 확인 전까지 미검증이다.
  이후 개별 요소를 가운데 정렬하지 않고 각 영역의 기존 내부 정렬을 유지한 채 콘텐츠 덩어리만
  중앙으로 이동했다. 시계는 왼쪽 16px·아래 8px, AI는 위 8px, 하드웨어는 위 20px 이동했으며
  `artifacts/azure-theme-preview-v6-20260911.png`에서 합성 결과를 확인했다. 이 조정본은 아직 실제
  패널로 전송하지 않았다.
- 소스·의존성·문서는 로컬 저장했으나 커밋하지 않았다. 미추적 소스·사용자 자산을 보존한다.
  `g1-azure-check.png`는 합성 확인용 임시 이미지로 남아 있으며 필수 입력이 아니다.


- 실시간 검증 도구의 첫 구현 범위는 `-background`의 로컬 파일 반복과 2초마다 바뀌는 진단
  오버레이, 25fps PNG 파이프 공급, Annex B stdout 전송이다. `-render-only`는 USB 없이 인코더
  출력만 새 파일에 기록한다. 기존 파일은 덮어쓰지 않는다. 시험은 기본 30초로 제한한다.
  실시간 청크는 협상된 길이를 전부 채운 경우에만 전송하고 종료 플래그를 붙이지 않는다.
  EOF·대기 상한 초과에서는 부분 청크를 보내지 않고 스트림을 정지한다. 이는 미검증 실험 경로이며
  PNG 폴백·재시도·제품 데몬은 이번 최소 도구에 포함하지 않는다. 오류는 진단과 종료 코드로 알린다.
  진단 글꼴에는 `golang.org/x/image/font/basicfont`를 쓰며 첫 테마 글꼴 선택과 구분한다.

- 설치 경로: FFmpeg `C:\Workspace\tools\ffmpeg-9.0.1-essentials_build`,
  MSYS2 `C:\Workspace\tools\msys64`. UCRT64 GCC·libusb·pkgconf 설치 완료.
  `README.md`의 PowerShell 환경 설정을 먼저 적용한다. 사용자 PATH를 영구 변경하지 않았다.
- 최종 코드에서 Windows `go test ./...`, `go vet ./...`, `go build ./...` 통과.
  명령 ID 응답 검사 보강 후 실제 sync 재검사도 통과했다.
  `go test -race ./...`는 Windows의 `asm.exe` 실행 거부, `-p 1` 재시도는 `gcc.exe`
  실행 거부로 완료하지 못했다. 일반 검사 통과와 구분하며 보안 기능을 해제하지 않았다.
- 유한 Annex B H264 검증 경로를 구현했다. 업스트림 3.10.0의 111/112/13/14/41/102/15/17,
  청크 121, 큐 상태 122, 정지 123 순서를 사용한다. 협상값이 1..1MiB가 아니면 202752바이트를
  쓰고, 큐 깊이 3 초과는 설정된 상한 안에서 기다리며 실패·취소 뒤에도 별도 제한시간으로 정지를 시도한다.
  단위 테스트는 명령 순서·밝기/fps·청크 크기/마지막 플래그·큐 대기·정지를 확인한다.
- `README.md` 환경 변수를 적용하고 작은 유한 Annex B H264 파일로
  `go run ./cmd/turzx-probe -h264 <경로>`를 실행했다. 사용자가 전체 화면 정상 재생을 확인했다.
  H264 모드는 기본 밝기를 32로 바꾼다.
  2026-09-11 실행에서는 sync 70ms, H264 초기화부터 정지까지 4875ms, 1,523,425바이트·8청크,
  최대 큐 값 4, 마지막 큐 값 3을 관측했다. 명령 17의 협상값은 0이어서 202752바이트 기본값을 썼다.
  17/121/122/123은 모두 명령 ID·C8 응답을 반환했고 종료 코드는 0이었다. 일반 사용자 여부는 이번
  실행에서 별도로 판정하지 않았다. 사용자가 진단 영상의 전체 화면 정상 재생을 확인했으며,
  마지막 프레임까지 재생 완료 여부는 별도로 확인하지 않았다.
  마지막 큐 값이 3 이하이면 곧바로 정지하는 현재 경로는 영상 끝까지 재생됐음을 보장하지 않는다.
  소재는 README 명령으로 만든 8초·200프레임의 진단 패턴(462×1920, yuv420p, libx264 High)이며
  SHA-256은 `8467c885a44285c0ac6e8eeb0768e8d2cb1d2925c3f8e8e99dd5f83a041f161d`다.
  기성 모션 배경이나 첫 테마가 아니며 G1 통과 기록으로 해석하지 않는다.
  첫 초기화 명령의 응답 실패 때도 제한시간을 둔 정지를 시도하도록 보강했다.
- FFmpeg 실시간 진단 오버레이와 stdout 청크 조립 대기 상한을 구현했다.
  PNG 디코더 프레임 스레딩 지연을 줄이기 위해 `-threads 1`을 적용하고, 저변화 구간의 출력량을
  유지하는 후보로 libx264 1500k CBR(`-minrate/-maxrate/-bufsize 1500k`, `nal-hrd=cbr:force-cfr=1`)을
  사용한다. H264 filler는 인코더가 생성하며 전송 계층에서 패딩하지 않는다.
  호스트 단독 22초 시험에서 움직이는 8초 배경과 정적 배경 각각 20개 청크를 만들었고,
  첫 청크 포함 최대 대기는 각각 약 1.17초·1.32초였다. 실제 표시 지연의 측정값은 아니다.
  Windows 패널 전송은 움직이는 진단 배경 30초에서 27청크·5,474,304바이트·최대 대기 1121ms,
  저변화 배경 15초에서 13청크·2,635,776바이트·최대 대기 1081ms를 관측했다.
  양쪽 모두 청크 크기 202752바이트·최대 큐 값 1이고, 대기 상한 초과 없이 지정 시간에 도달했다.
  기간 만료 취소와 명령 123 C8 정지 응답을 확인했다. 현재 CLI는 기간 만료도 오류로 반환해
  종료 코드가 1이며, `duration_reached=true`와 다른 오류의 유무로 구분한다.
  실시간 패널 화면은 사용자가 관찰하지 못했으므로 정상 표시·카운터 갱신은 아직 미확인이다.
  두 소재의 SHA-256은 각각 `a8c72a654cb416f0f5c3ae9ee422a389af1aee15acfd796ba6aae64e2af10c0f`,
  `25f02bf193b814c408c0d879a15c2a47366ff932ceee18c059c518c3c3bf8304`다.
  첫 바이트/부분 청크 정체·USB 전송 중 다음 청크 대기 초과·취소·정지 오류 보존을 테스트한다.
  Windows의 일반 테스트·vet·build와 render/turzx race 검사를 통과했다.
  유한 파일이나 진단 패턴의 짧은 실행만으로 G1을 통과 처리하지 않는다.
- 사용자가 제공한 실제 배경은 `assets/backgrounds/azure-ribbon.mp4`다. H264 High, 1920×462,
  yuv420p, 30fps, 8초이며 합성 출력은 25fps로 변환한다.
  SHA-256은 `7b6e83e821f3352e98ceb082e5224a4dbce8ad4f37c4a7b4d516ad38638291f1`이다.
  20초 호스트 합성 시험의 출력 488프레임(19.52초)을 오류 없이 디코딩했고, 두 번의 반복 경계 뒤
  진단 카운터가 합성된 가로 화면을 확인했다. 1분 패널 시험은 55청크·11,151,360바이트,
  최대 대기 1162ms·최대 큐 값 1·정지 응답 C8이었다. 사용자가 영상·카운터 정상 표시를 확인했다.
  이후 30분 자원 측정을 시작했으나 사용자 요청으로 약 75초에 중단했다. 이 실행에서는 생성한
  FFmpeg 자식만 종료하고 probe의 EOF 처리·장치 정지·프로세스 정리를 확인했다. 장시간 통과 기록은 아니다.
  중단 전 워밍업 이후 표본의 Go+FFmpeg 평균 CPU 약 3.35%(호스트 전체 기준), 최대 working set
  약 181MiB·private 약 292MiB였다. 짧은 관측이므로 정상 운용 자원 보장으로 해석하지 않는다.
  실제 패널 표시 지연의 수치 검증은 별도다.
  진단 화면 배치는 시안이 아니다. 별도의 `azure-ribbon` 코드 시안은 H264 합성 프레임까지 확인했고
  실제 패널 가독성과 실데이터 연결 전에는 확정 테마로 취급하지 않는다.
- 현재 변경은 로컬 파일에 저장됐으며 커밋하지 않았다. 착수 전부터 `AGENTS.md`, `CLAUDE.md`,
  `DESIGN.md`, `internal/turzx/packet.go`의 수정과 미추적 `.idea/`가 있었다.
  전체 diff를 이번 구현 변경으로 간주하거나 기존 수정을 되돌리지 않는다.
- 작업 방식은 작은 독립 구현만 하위 에이전트에 위임하고 주 에이전트가 통합·검증하는 방향이다.
  큰 문맥을 여러 에이전트가 중복 탐색하도록 나누지 않는다.
- 필수 `ponytail` 스킬은 사용자 범위
  `C:\Users\youhyun\.codex\skills\ponytail\SKILL.md`에서 읽고 적용했다. 공통 `CLAUDE.md`는 지정된
  `C:\Workspace\CLAUDE.md`와 `C:\Users\youhyun\workspace\CLAUDE.md` 모두 없었다.

### G0~G2 필수 재현 사례

아래는 실행할 검증 목록이며 통과 기록이 아니다. G0의 실제 입력·계정 식별 결과는 개인정보를
제거한 fixture로 만들고, G2에서 가짜 시계·파일 입력·지연 응답으로 상태 전이를 검사한다.

| 단계 | 재현 상황 | 기대 결과 |
|---|---|---|
| G0 | Windows 또는 WSL의 로그인 계정을 A→B로 바꾸고 같은 별칭 사용 | 공급자별 자동 감지 범위를 확인. 감지 불가 경로는 수동 확인·새 연결 세대·새 세션 절차로 재연결 |
| G0 | Codex의 같은 인증 모드 내 계정 변경, 계정 정보 부재, 사용량 조회 도중 인증 변경 | `account/read`와 알림의 실제 동작 확인. 확인 불가능한 결과를 이전 계정 값으로 채택하지 않음 |
| G1 | 실제 배경의 저변화 구간을 길게 유지하고 오버레이만 2초마다 교체 | 청크 생성량·대기시간을 측정. 해당 구간에서도 청크 대기 상한과 실제 표시 지연 목표 충족 |
| G1 | 배경을 반복하고 루프 경계 직전·직후에 지표 변경 | 반복 경계에서 멈춤·PTS 누적·지표 표시 지연 증가 없음. 정상 30분 검증은 폴백/재시작 없이 통과 |
| G1 | 첫 stdout 바이트 지연, 청크 도중 출력 중단, USB 송신/응답 정체를 각각 주입 | 추가 바이트 없이도 대기 상한을 감지. I/O 제한시간 후 직렬 복구, PNG 전환 또는 장치 오류 표시. 재시도 소진 후 루프 중단 |
| G1 | 짧은 청크 허용 여부와 저출력 시 인코더 설정 비교 | 검증된 전송 규칙·최종 청크 대기 상한·인코더 설정을 확정. 목표 불충족은 G1 실패 |
| G2 | 유효 창 → 리셋 경과 → 성공 입력에서 창 누락/null → 새 미래 리셋 창 | `갱신 대기` 유지 후 새 창에서 해제. 0% 사용량을 누락과 구분하고 오래된 창으로 역행하지 않음 |
| G2 | 처음부터 창 없음, 최초 입력 전, 리셋 전 일시 누락, 리셋 대기 중 인증 오류/명시적 지원 종료 | 각각 미제공·수집 중·과거 값·인증 필요/미제공. 인증 오류 해제 후 새 창이 없으면 갱신 대기로 복귀 |
| G2 | 한 창만 갱신하거나 계정 재연결 뒤 이전 세대 응답/파일 도착 | 다른 창의 시각을 갱신하지 않음. 이전 세대 값·리셋 이력·진행 중 응답을 새 연결에서 사용하지 않음 |
| G2 | 데몬 재시작, 실행 중 오래된 세션 파일 첫 발견, 동일 파일 재스캔 | 첫 파일은 기준 스냅샷. 원래 수신 시각을 모르면 null이며 정상으로 승격하지 않음 |
| G2 | 같은 내용의 높은 순번, 동일/낮은 순번, 부분 JSON, 과거 로그 재탐색 | 높은 순번의 유효 훅만 해당 창의 수신 시각 갱신. 내용 변경/원본 시각은 유지. 나머지는 신선도를 갱신하지 않음 |

### 제안하는 통과 목표 — 실측값 아님

- **지연:** 데몬이 새 샘플을 받아들인 때부터 패널에서 바뀔 때까지 p95 ≤3초, 관측 최대 ≤5초.
  공급자가 새 값을 내기까지의 시간은 별도로 기록한다. 호스트 시각/카운터와 실제 화면을 촬영해
  비교하고 전송 완료 시간으로 대체하지 않는다. 표본은 실행 초반·중간·말과 저변화 구간·반복
  경계에서 수집한다. 지표 변경 시점을 2초 렌더 주기와 다른 위상으로 분산해 갱신 대기도 포함한다.
- **영상:** 우선 25fps 입력으로 실제 배경과 반복 경계를 확인한다. 색 깨짐·멈춤·누적 지연이 없어야
  하며 파일 인코딩 fps만으로 통과하지 않는다. 높은 fps 탐색은 이 조건 이후다.
- **복구:** 분리/재연결과 절전/복귀 각 10회. 정상 장치가 다시 접근 가능해진 뒤 30초 이내 출력
  회복을 목표로 한다. 실패 시 원인과 수동 복구 필요를 보여야 하고 재시도 루프가 폭주하면 안 된다.
- **자원:** 사무실 PC 정상 운용에서 Go+ffmpeg 합계 평균 CPU ≤전체 용량의 5%, 메모리 ≤300MiB를
  초기 예산으로 둔다. LHM·Codex App Server 추가 비용도 따로 보고한다. 워밍업 후 메모리와 대기열이
  계속 늘지 않아야 한다. 초과 시 인코더·입력 경로를 조정하고 실제 비용으로 예산을 재검토한다.
- **데이터:** 원본이 제공되는 모든 요구 항목을 표시한다. 지원되지 않는 항목은 설치 단계에서
  식별하고 명시적으로 표시하며 가짜 0·100%로 통과시키지 않는다.

G1이 실패하면 비트레이트·청크·버퍼·인코더를 조정한다. 그래도 지연을 만족하지 못하면 PNG로
데이터 경로를 계속 검증하되 **모션그래픽 요구까지 완료됐다고 처리하지 않는다**.
첫 테마의 모션 요구를 변경해야 할 때는 측정 결과와 가능한 대안을 사용자에게 제시한다.

측정에는 OS/CPU/GPU·USB 경로·라이브러리 버전·전체 인코더 설정·소재 해시·실행 시간,
송수신 오류·재시작 횟수·CPU/RAM·출력 bytes/s·청크 조립/큐 대기시간·대기 상한 초과·표시 지연을
남긴다. 정상 연속 실행과 정체 주입/복구 실행의 결과를 구분한다. 지속할 재현 도구와 테스트만
저장소에 두고 토큰·쿠키·대화 원문은 기록하지 않는다.

이 문서는 한 개발 사이클용이다. 구현·배포 검증이 끝나면 코드로 알 수 없는 지속 규칙만
`AGENTS.md`에 옮기고 삭제한다. 별도 영구 리뷰 이력이나 중복 진행 문서는 만들지 않는다.

[upstream]: https://github.com/mathoudebine/turing-smart-screen-python/blob/3.10.0/library/lcd/lcd_comm_turing_usb.py
[claude]: https://code.claude.com/docs/en/statusline
[claude-auth]: https://code.claude.com/docs/en/authentication
[codex]: https://learn.chatgpt.com/docs/app-server
[filters]: https://ffmpeg.org/ffmpeg-filters.html#overlay
[ffmpeg]: https://ffmpeg.org/ffmpeg.html#Advanced-options
[go-png]: https://go.dev/src/image/png/writer.go
[lhm]: https://github.com/LibreHardwareMonitor/LibreHardwareMonitor
[lhm-http]: https://github.com/LibreHardwareMonitor/LibreHardwareMonitor/blob/master/LibreHardwareMonitor.Windows.Forms/Utilities/HttpServer.cs
[lhm-pawnio]: https://github.com/LibreHardwareMonitor/LibreHardwareMonitor/blob/v0.9.6/LibreHardwareMonitorLib/PawnIo/PawnIo.cs
[dotnet-deploy]: https://learn.microsoft.com/en-us/dotnet/core/deploying/
[gopsutil]: https://github.com/shirou/gopsutil
[gopsutil-win]: https://github.com/shirou/gopsutil/blob/master/sensors/sensors_windows.go
[ghw]: https://github.com/jaypipes/ghw#inspecting--monitoring
[nvml]: https://github.com/NVIDIA/go-nvml
[gousb]: https://github.com/google/gousb
[libusb-win]: https://github.com/libusb/libusb/wiki/Windows
[font]: https://pkg.go.dev/golang.org/x/image/font/opentype
[gg]: https://github.com/fogleman/gg
[systray]: https://github.com/getlantern/systray
[go-layout]: https://go.dev/doc/modules/layout
[go-review]: https://go.dev/wiki/CodeReviewComments
[smartscreen]: https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/smartscreen-reputation
[win-signing]: https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/code-signing-options
[artifact-signing]: https://learn.microsoft.com/en-us/azure/artifact-signing/quickstart
[signtool]: https://learn.microsoft.com/en-us/windows/win32/seccrypto/signtool
[apple-notary]: https://developer.apple.com/documentation/security/notarizing-macos-software-before-distribution
[apple-workflow]: https://developer.apple.com/documentation/security/customizing-the-notarization-workflow

장치 대조용 추가 자료: [업스트림 3.10.0 릴리스](https://github.com/mathoudebine/turing-smart-screen-python/releases/tag/3.10.0),
[USB 하드웨어 지원 논의 #727](https://github.com/mathoudebine/turing-smart-screen-python/issues/727),
[하드웨어 리비전](https://github.com/mathoudebine/turing-smart-screen-python/wiki/Hardware-revisions),
[별도 CLI 구현](https://github.com/phstudy/turing-smart-screen-cli),
[제조사 소프트웨어·테마](https://www.turzx.com/2025/05/26/88_inch/).
