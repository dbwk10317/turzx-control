# AGENTS.md — turzx-control

## 로딩 순서

이 프로젝트에서 작업하기 전에 아래를 넓은 것에서 좁은 것 순으로 **모두** 읽고 따른다.

1. `~/workspace/CLAUDE.md` — 워크스페이스 공통 규칙
2. `~/workspace/turzx-control/CLAUDE.md` — 이 프로젝트의 공유 원본
3. `~/workspace/turzx-control/DESIGN.md` — 장치 실측값과 설계 근거

`CLAUDE.md`가 프로젝트 맥락·명령·제약·컨벤션의 단일 공유 원본이다. 이 파일은 그것을 다시
쓰지 않는다. 위 경로는 산문 언급이 아니라 실제로 읽어야 할 파일 목록이다.

## Codex 전용 메모

아직 없다. Codex에서만 참인 사실(샌드박스·승인 동작, 이 프로젝트에서 통한 `codex exec` 옵션,
겪은 도구 차이)이 생기면 여기에 쓰고 `CLAUDE.md`에는 쓰지 않는다.
