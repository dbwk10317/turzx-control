# turzx-control 작업 지침

이 문서는 공통 규칙의 원본이자 Codex 진입점이다. 먼저 `../AGENTS.md`를 읽고, 없으면 `../CLAUDE.md`, `~/workspace/CLAUDE.md` 순서로 확인한다. 모두 없으면 누락을 알리고 이 문서로 진행한다. 이 문서를 끝까지 읽고 `PROGRESS.md`가 있으면 남은 작업을 확인한다. Claude 전용 지침만 `CLAUDE.md`에 둔다. 영구 worklog는 만들지 않으며 진행 문서는 해당 작업 완료 시 삭제한다.

## 프로젝트 계약

TURZX 스마트 스크린용 크로스플랫폼 Go 데몬이다. Windows 네이티브 데몬이 USB, 트레이, 센서, 렌더링을 소유한다. WSL의 프로필이나 센서 값을 선택·병합하지 않는다.

AI 사용량은 Codex와 Claude 각각 데몬 전용 프로필에서 수집한다. 제품 코드가 자격 증명을 직접 해석하지 않으며 설정 UI에서 로그인을 시작한다. 공급자 값은 provider·binding·account identity·generation을 함께 대조하고, 연결 해제·재연결과 계정 변경을 별도 상태로 처리한다. 표시 지표는 각 공급자의 5시간·주간 남은 사용량과 리셋까지의 시간, CPU·GPU·RAM 사용률·온도다. RAM 온도를 지원하지 않으면 식별된 메인보드 온도로 대체하고 실제 출처를 표시한다. 일시적 센서 오류, 미제공 창, stale 값, 인증 오류를 서로 구분하며 과거 값으로 새 관측을 만들지 않는다. reset은 유효한 관측의 리셋 시각 전이로만 갱신한다.

하드웨어 수집과 화면 합성은 1초, 공급자 원본 조회는 30초 주기다. 사용량 값은 조회 완료 시 원자적으로 교체하며 화면 재표시는 새 관측이 아니다. 센서 snapshot IPC는 관리자 helper가 고정된 `%ProgramData%\TURZXControl\Sensors\<SID>\snapshot.json`에 기록하고, Go는 protocol·관측 시각·신선도·Elevated·regular/non-reparse 파일·handle 및 ACL을 검증한다. 부모 디렉터리는 보호된 DACL이어야 하고 owner는 `BUILTIN\Administrators` 또는 `SYSTEM`이며 비신뢰 principal의 쓰기·삭제·DACL·owner 권한은 거부한다.

USB 응답은 항상 읽는다. 시작 시 IN 큐를 비우고 stale 응답이 쌓이면 IN 큐를 비운 뒤 sync를 다시 보낸다. PNG 전송 데이터는 8-bit RGBA이며 IHDR color type 6을 확인한다. 스트림 중단·EOF·장치 오류에서는 정지와 응답을 처리하고 재연결 경로를 유지한다.

기본 테마 ID는 `smon-halloween`이다. 배포물은 필요한 실행 파일과 런타임을 동봉한 standalone ZIP을 목표로 하며 Windows 실행 파일과 동봉 코드는 Authenticode 서명·SmartScreen 평판을 고려한다. macOS 실행 코드는 서명·공증을 고려한다. 미서명 개발본을 정식 배포본으로 취급하지 않는다.

GPL-3.0-or-later를 따른다. 새 소스 파일에는 SPDX 헤더와 필요한 출처 고지를 둔다.

## 데이터와 프로세스 불변 조건

- 연결마다 새 `binding_id`를 발급한다. 계정 전환·연결 해제 때 이전 값과 리셋 이력을 비우고 이전 세대 파일과 늦게 완료된 조회를 거부한다. Codex는 사용량 조회 전후 `account/read`로 계정을 대조한다. Claude는 수동 확인한 계정이며 통보 없는 계정 변경 자동 감지를 약속하지 않는다. 재연결 후에는 새 세션을 사용하고 기존 훅의 세대를 바꾸지 않는다.
- Claude inbox는 공급자·환경·계정 별칭·세션별로 분리하고 같은 디렉터리에서 원자 교체한다. 스키마·연결 세대·세션·단조 증가 순번을 검증한다. 최초 발견과 데몬 재시작에서는 파일을 과거 baseline으로만 읽고, 이후 더 큰 순번만 새 수신으로 인정한다. 중복·부분 JSON·이전 세대 파일·mtime은 신선도를 갱신하지 않는다.
- `source_observed_at`, `received_at`, `last_changed_at`을 구분하고 모르는 시각은 null로 둔다. 공급자·계정·버킷을 합산·평균·최댓값으로 병합하지 않는다. 각 창의 신선도는 별도로 유지한다. 초기 오래됨 기준은 센서 10초, Codex 성공 조회 180초, Claude 유효한 새 훅 수신 5분이며 공급자의 갱신 보장이 아니다.
- 리셋 시각이 지나면 `갱신 대기`다. 누락·null·사용률 감소만으로 새 창이나 100% 복구를 만들지 않는다. 같은 범위에서 더 뒤의 유효한 리셋 시각과 사용률을 새 관측으로 받아야 해제한다. 처음부터 제공되지 않은 창만 성공 응답의 누락을 `미제공`으로 판정한다. 인증·계정·수집 오류는 원인을 우선 표시하되 같은 연결의 리셋 이력은 보존한다.
- 남은 비율은 `clamp(100-used_percent, 0, 100)`이며 누락과 0을 구분한다. 음수·비수치·단위 오류는 거부한다. 리셋 잔여 시간은 epoch 기준으로 계산하고 음수를 표시하지 않으며 절전 복귀·시계 변경 때 재계산한다.
- Codex는 전용 App Server의 `account/rateLimits/read`를 사용하며 대화·모델 호출을 만들지 않는다. 버킷과 실제 창 길이(300분·10080분)를 검사하고 다른 버킷으로 조용히 대체하지 않는다. Claude는 공식 statusline 허용 필드만 저장하고 기존 출력을 보존·복원한다. 인증 파일 파싱, 내부 OAuth 엔드포인트, TUI 스크래핑은 사용하지 않는다. 유휴 Claude의 실시간 갱신은 보장하지 않는다.
- 센서는 ID로 선택하며 RAM 미지원이 확인된 경우에만 사용자가 식별한 메인보드 온도로 대체한다. ACPI 서멀존을 CPU 온도로, 임의 VRM·칩셋을 RAM 온도로 바꾸지 않는다. 권한·드라이버 부족은 미지원과 구분한다. helper는 센서 읽기만 수행하고 수집 오류는 항목별로 격리한다.

## 렌더링·장치·배포

- 확인된 장치는 VID `0x1cbe` / PID `0x0092`, 네이티브 462×1920이다. 1920×462 화면을 시계 방향 90도로 회전한다. 다른 모델의 크기·기능을 PID로 추측하지 않는다. 밝기 범위는 0–102다.
- 합성은 호스트에서 수행한다. 오버레이 갱신 1초와 FFmpeg 프레임 공급 25fps를 구분한다. H264 Annex B를 사용하며 지표마다 FFmpeg를 재시작하지 않는다. 입출력·stderr 배출을 서로 막지 않게 하고 취소 시 파이프·자식을 정리한다.
- USB 소유자는 하나이며 트랜잭션은 직렬화한다. 청크 조립 시작부터 USB 송신 시작까지 첫 바이트 대기·조립·큐 대기를 포함해 제한한다(기본 3초). 검증되지 않은 짧은 청크·패딩·압축 스트림 중간 폐기는 금지한다. 중단 시 정지 응답·drain·sync 후 복구하고 이전 스트림 바이트를 재사용하지 않는다.
- 영상 실패는 PNG로 전환하고 1·2·4초 간격으로 최대 3회 재시도한다. 재연결로 예산을 초기화하지 않으며 정상 영상 30분 후 초기화한다. USB 재연결 backoff는 1~30초다. 보류된 장기 실험을 자동 재개하지 않는다. 측정 목표·계산값과 실측을 구분한다.
- LHM GUI나 별도 .NET 설치를 요구하지 않도록 self-contained helper를 동봉한다. 메인 앱 전체를 상시 관리자로 실행하거나 OS 보안 기능을 해제하지 않는다. 관리자 helper는 보호된 설치 경로·작업·snapshot ACL을 검증하고 동일 사용자 SID의 최소 권한으로 연결한다.
- PawnIO는 공식 버전을 고정해 출처·해시·Authenticode를 검증한 동봉 설치본을 사용한다. 최초 설정에서 센서 활성화를 선택하면 UAC 후 설치 결과와 새 helper의 실제 센서 상태를 확인한다. 취소·실패·재부팅 필요를 구분하며 기본 사용률은 계속 제공한다. 공유 드라이버를 앱 제거 시 무조건 삭제하지 않는다.
- 빌드 통과와 배포 실행 검증을 구분한다. FFmpeg 빌드 옵션을 포함해 동봉 의존성의 라이선스·고지·corresponding source 조건을 확인한다. 드라이버 서명·해시와 깨끗한 PC 실행은 별도 검증한다.

## 구현 규칙

- Go 표준 구조와 Code Review Comments를 따른다. 작은 구체 타입, 소비자 쪽의 작은 인터페이스, 명시적 오류·취소·goroutine 생명주기를 사용한다.
- 기존 helper와 표준 라이브러리를 먼저 찾고 새 의존성·추상화·보일러플레이트를 추가하지 않는다. 입력 검증·오류 처리·보안 검사는 생략하지 않는다.
- 기존 미커밋 변경, 사용자 제공 `assets/`, 측정 원본, `artifacts/`를 덮어쓰거나 삭제하지 않는다. `golden.json`은 근거 없이 바꾸지 않는다.
- 장치 검증은 직렬로 수행하며 실제 장치 응답, 단위 테스트, 실제 패널 표시 확인을 서로 구분한다.
- 코드 작업에는 `ponytail` 스킬을 필수 적용한다. 현재 환경의 스킬 목록과 실제 경로를 확인하며, 없으면 사용했다고 주장하지 말고 제약을 보고한다. Go 관례가 우선한다.
- 변경한 Go 파일에 `gofmt`를 적용하고 기본적으로 `go vet ./...`, `go test ./...`, `go build ./...`를 수행한다. 동시성 변경은 지원 환경에서 race 검사를 추가한다. 실행하지 못한 검사는 이유를 보고한다.

## 에이전트와 보고

메인 에이전트가 요구사항 해석, 설계, 분할, 통합, 최종 검증을 책임진다. 독립 작업에만 custom agent를 사용하며 목표·파일 범위·완료 조건·검증 방법을 전달한다. 탐색은 `spark_explorer` 읽기 전용, 구현은 `spark_worker`, 중요한 변경 리뷰는 `reviewer` 읽기 전용으로 맡긴다. 모델 quota가 소진되면 같은 모델을 반복하지 않고 다음 가용 후보에 인계한다. `reviewer`의 `gpt-5.6-terra`가 없으면 `gpt-6-astra`를 별도 읽기 전용 reviewer로 사용한다. 고정 모델 custom agent에 강제로 다른 모델을 지정하지 않으며, 모델 override 시 `fork_turns: "none"` 또는 허용된 양의 정수를 사용한다. 지정 모델이 없으면 실제 사용 모델을 정확히 보고하고 역할과 읽기 전용 제약을 유지한다. 같은 파일을 병렬 수정하지 않는다.

사용자에게 진행 상황과 결과를 한국어로 설명한다. 코드 식별자와 명령어는 원래 표기를 유지한다. 변경 내용, 검증 결과, 남은 문제를 보고한다.

## Codex 도구 규칙

도구와 셸에서 확인한 차이는 이 문서에 기록하되 세션 권한을 영구 허용으로 적지 않는다. 파일 검색은 `rg`/`rg --files`를 우선한다. 독립적인 읽기·검색은 병렬화하고 의존 작업·변경은 순서대로 수행한다. 파일 수정은 `apply_patch`를 사용한다. 파괴적 작업은 정확한 대상을 먼저 확인하고 명시적 요청 없이는 수행하지 않는다. `$HOME`, `$CODEX_HOME` 같은 공통 환경 변수는 작업 변수로 재사용하지 않는다.

Windows에서 Go가 PATH에 없으면 `C:\Program Files\Go\bin\go.exe`를 사용한다. Windows cgo·libusb 검증은 `README.md`의 환경 설정과 아래 로컬 개발 환경 절의 경로를 적용한다. 완료된 설계와 날짜별 작업 이력은 유지 문서에 복사하지 않는다.

## 로컬 개발 환경

저장소 밖에 있는 외부 도구의 실제 위치다. 환경마다 다르므로 `README.md`에는 자리표시자만 두고 여기서 관리한다. 다른 PC에서 작업하면 이 절을 그 PC의 경로로 갱신한다.

- MSYS2 UCRT64: `C:\Workspace\tools\msys64` (GCC·`pkg-config`·libusb는 `ucrt64\bin` 아래)
- FFmpeg: `C:\Workspace\tools\ffmpeg-9.0.1-essentials_build\bin\ffmpeg.exe`
- Go 검증 전 셸에서 `PATH` 앞에 `ucrt64\bin`을 두고 `CGO_ENABLED=1`, `CC`, `PKG_CONFIG`를 위 경로로 지정한다. 이 설정 없이는 `internal/turzx`·`internal/daemon`·`cmd/turzx-control`·`cmd/turzx-probe`가 빌드되지 않고 `-race`를 쓸 수 없다.
