# AGENTS.md — turzx-control

이 파일은 **프로젝트 공통 규칙의 원본이자 Codex 진입점**이다.
공통 규칙과 Codex 전용 규칙은 여기에 기록하고, Claude 전용 지침만 [CLAUDE.md](CLAUDE.md)에 둔다.
아래 공통 규칙은 모든 에이전트에 적용하며, 마지막 Codex 전용 절은 Codex에만 적용한다.
구현 전 설계 결정과 현재 재개 지점은 [DESIGN.md](DESIGN.md)에 유지한다.

## 반드시 읽을 순서

1. 워크스페이스 공통 지침은 `../AGENTS.md`를 먼저 확인한다. 없으면 기존 공통 지침 경로인
   `../CLAUDE.md`, `~/workspace/CLAUDE.md` 순서로 확인한다. 모두 없으면 누락을 알리고
   이 저장소 지침으로 진행한다. 다른 저장소의 파일로 대체하거나 공통 규칙을 추측하지 않는다.
2. 이 파일의 공통 규칙을 끝까지 읽는다. Codex는 Codex 전용 절도 따른다.
   Claude는 자신의 진입점인 `CLAUDE.md`의 전용 지침을 함께 따른다.
3. [DESIGN.md](DESIGN.md)를 끝까지 읽는다. 설계 종료 후 제거되었다면 이 파일의 갱신된 참조를 따른다.

프로젝트 파일은 실제 체크아웃을 기준으로 찾는다. macOS의 `~/workspace/turzx-control`을
Windows의 홈 디렉터리 아래 경로로 고정해서 해석하지 않는다.

## 프로젝트 맥락

TURZX / Turing USB 스마트 스크린 패널에 Claude·Codex 사용량과 시스템 센서를 띄우는
크로스플랫폼 데몬. Go로 만들고 Windows·macOS·Linux를 지원하는 것이 목표다.

개발은 Mac, 실제 운용은 사무실 Windows PC다. 사무실에서는 Windows와 WSL 양쪽에서
Claude·Codex를 사용한다. USB와 호스트 센서는 Windows 네이티브 데몬이 소유한다.
사용량의 출처·계정·신선도 계약은 `DESIGN.md` 4절을 읽는다.
첫 테마의 AI 사용량은 Windows 트레이 데몬이 관리하는 공급자별 전용 프로필만 사용하며
기존 Windows 앱·CLI 프로필과 WSL 관측은 선택·병합하지 않는다. 로그인은 설정 UI에서 시작하되
자격 증명은 Codex App Server와 Claude Code가 각 전용 프로필에 저장하고 제품 코드가 직접 해석하지 않는다.
표시 지표는 Codex·Claude 각각 5시간/주간 남은 사용량과 각 리셋까지의 시간,
CPU·GPU·RAM 사용률과 온도다. RAM 온도가 미지원이면 식별된 메인보드 온도로 대체하고
실제 출처를 라벨에 표시한다. 일시적 센서 오류는 미지원으로 취급하지 않는다.
하드웨어 수집과 전체 화면 합성은 1초 간격, Claude·Codex 사용량 원본 조회는 30초 간격이다.
AI 값은 조회 완료 때 최신 스냅샷으로 원자 교체하고 1초 화면 합성에서 그대로 읽는다.
사용량 원본의 조회·훅 수신은 별도이며 화면 재표시만으로 새 관측으로 취급하지 않는다.

## 현재 상태

**G0/G1 검증 도구 구현 착수, 제품 데몬 미구현.** `internal/turzx/`에는 패킷 생성·golden 테스트와
장치용 RGBA PNG 인코딩·검사가 있다. `scripts/check-windows.ps1`은 읽기 전용 환경 진단이다.
`cmd/turzx-probe`는 gousb/libusb 기반 최소 sync·PNG 전송 검증 도구다.
유한 Annex B H264의 초기화·청크 협상·큐 조회·정지 경로를 구현하고 Windows 장치 응답까지 확인했다.
사용자가 진단 영상의 전체 화면 정상 재생을 확인했다. 마지막 프레임까지의 재생 완료와 실시간 지연은 미검증이다.
실시간 FFmpeg 진단 오버레이·청크 대기 상한 경로를 구현했다. 실제 기성 배경으로 G1을 검증하는 단계이며,
CPU·RAM 사용률 수집기와 1초 간격 JSON 진단 CLI(`cmd/turzx-metrics`)가 있다.
LHM 라이브러리 기반 동봉 센서 보조 프로그램과 Go 진단 CLI 연동이 있으며, Windows 일반 사용자
권한에서 RTX 5060 사용률·코어 온도를 확인했다. CPU·RAM/메인보드 온도는 드라이버·권한·센서
선택 검증이 남아 있다. AI 사용량 연동과 제품 데몬은 아직 없다. Windows 빌드 환경 설정은 `README.md`를 따른다.

Windows 일반 사용자 권한의 sync·PNG 전송과 사용자의 실제 패널 색상 띠 정상 표시 확인까지
완료했다. G0/G1 전체 통과는 아니다. 현재 검증 결과·미완료 항목·재개 순서는 `DESIGN.md` 8절에 있다.

실제 azure-ribbon 배경과 LIVE 카운터는 1분간 패널에서 정상 표시됨을 사용자가 확인했다.
사용자 결정으로 30분 연속 테스트는 중단·보류했고 구현의 선행 조건에서 제외했다.
배포는 실행 파일·필요 구성요소를 동봉한 standalone ZIP을 목표로 하며 LHM GUI 설치를 요구하지 않는다.
다음 단계는 CPU·RAM/메인보드 온도의 드라이버·권한 검증과 Claude·Codex 사용량 연동이다. 장시간 안정성·표시 지연 수치 검증은
후속 과제로 남긴다. 재개 시 `DESIGN.md` 8절의 현재 작업 재개 지점부터 확인한다.

## 첫 테마 디자인 상태

제공된 `assets/backgrounds/azure-ribbon.mp4`를 기준으로 시계, `AI 에이전트`, `하드웨어 모니터`
영역의 첫 코드 시안을 구현했다. `-theme azure-ribbon`은 실제 데이터 연결 전 레이아웃 검증용이며
화면에 `미리보기 데이터`를 표시한다. 기존 LIVE 카운터 화면과 과거 샘플은 여전히 처리량 진단용이다.
첫 시안의 실제 패널 가독성 확인과 데이터 연결 전에는 완성 테마로 취급하지 않는다.

## 반드시 먼저 읽을 것

- [`DESIGN.md`](DESIGN.md) — 장치 실측값, 프로토콜, 아키텍처 결정과 근거.
  과거 관측·코드 검증·설계 목표·미검증 후보를 구분한다. 수치를 추측하거나 목표를 실측으로 적지 않는다.
  4절 데이터 계약, 5~6절 스트림·전송 제약, 7절 Go 관례·라이브러리·배포 계획을 먼저 확인한다.

## 주요 경로와 변경 범위

- `cmd/turzx-probe/`: USB 전송·실시간 영상 검증 CLI.
- `cmd/turzx-metrics/`: USB 없이 하드웨어 지표를 수집하는 진단 CLI.
- `internal/metric/`: 하드웨어 지표 수집과 상태 처리.
- `internal/render/`: 진단 오버레이·FFmpeg 합성 및 인코딩.
- `internal/turzx/`: 장치 패킷·PNG 인코딩·USB 전송.
- 테스트는 각 패키지의 `*_test.go`, 기준 패킷은 `internal/turzx/testdata/golden.json`에 둔다.
- `scripts/`: 환경 진단과 선택적 스트림 측정 도구. 제품 데몬·설정 UI는 아직 미구현이다.

기존 미커밋·미추적 변경, 사용자 제공 `assets/`, 측정 원본과 `artifacts/`의 기존 산출물을
작업 범위 밖에서 덮어쓰거나 삭제하지 않는다. 생성물은 가능하면 생성 원본·절차를 수정해
재생성하고, `golden.json`은 테스트 통과만을 위해 바꾸지 않는다. 기준 패킷 변경에는
업스트림 또는 장치 검증 근거를 남긴다.

## 에이전트 활용

- 메인 에이전트는 요구사항 해석, 설계 판단, 작업 분할, 결과 통합과 최종 검증을 책임진다.
  범위가 명확한 탐색은 `spark_explorer`, 작은 수정과 독립적인 구현은 `spark_worker`를
  우선 활용하되, 위임 여부와 분할 방식은 메인 에이전트가 판단한다.
- 이미 문맥을 확보한 극소규모 수정이나 분리·통합 비용이 더 큰 작업은 직접 처리할 수 있다.
  단순한 작업이라는 이유만으로 Spark 활용을 배제하지 않는다.
- `spark_explorer`는 실행 흐름, 기존 구현 패턴과 관련 테스트를 읽기 전용으로 조사한다.
  같은 범위를 여러 에이전트에게 중복 조사시키지 않는다.
- `spark_worker`는 요구사항과 담당 파일이 명확한 구현 및 관련 검증을 맡는다.
  기존 Go 구조와 프로젝트 규칙을 따르고, 다른 작업자의 변경을 보존하며 함께 작업 중임을 전달한다.
- 위임할 때 목표, 필요한 맥락, 담당 파일, 완료 조건과 검증 방법을 명시한다.
- 위임이 지연을 줄이거나 메인 문맥을 절약하면 적극 활용한다. 파일·심볼 검색, 실행·데이터 흐름,
  패턴·테스트 확인처럼 범위가 좁은 읽기 전용 조사는 `spark_explorer`에 맡기고,
  서로 의존하지 않는 조사는 병렬화한다. 넓은 아키텍처 판단은 메인이 맡는다.
- `spark_worker`에는 접근 방법이 정해진 함수·소수 파일 수정, 테스트, 타입 수정,
  기존 패턴을 따르는 구현과 기계적 리팩터링을 맡긴다. 제약과 기대 동작도 함께 전달한다.
  원인 불명, 요구사항 모호, 여러 시스템의 조율, 보안·동시성·데이터 일관성 판단이 필요한 작업을
  막연히 조사하고 고치라고 맡기지 않는다. 메인이 먼저 판단한 뒤 독립적인 구현 단위만 위임한다.
- USB 전송·복구, 인증·권한과 계정 연결, 데이터 신선도, Windows/WSL 경계,
  프로세스 생명주기, 설정·테마 형식과 공통 아키텍처 변경은 메인이 먼저 설계와 영향을 검토한다.
- 서로 다른 파일을 수정하는 독립 작업만 병렬로 진행한다. 공유 계약이 필요하면 먼저 정하되,
  위임을 위해 불필요한 인터페이스를 만들지 않는다. 실제 USB 장치 검증은 직렬로 진행한다.
  동시 실행 수는 공통 지침과 현재 환경의 한도를 따른다.
- 중요한 동작 변경은 `reviewer`의 읽기 전용 검토를 활용한다. 메인이 위험도와 변경 범위에 따라
  필요성을 판단하고 정확성·회귀·보안·테스트 누락을 검토 범위로 지정한다.
  특히 인증·권한, 영속 데이터, 동시성, 외부 공개 API, 비단순 리팩터링, 회귀 위험이 높은 수정과
  여러 작업자의 병렬 구현은 독립 리뷰를 검토한다. 극소규모 기계적 수정은 메인의 직접 리뷰로 충분하다.
- 메인은 서브에이전트의 변경과 검증 근거를 직접 확인하고 필요한 프로젝트 검사를 수행한다.
  발견된 문제를 해결하고, 해결하지 못한 문제와 미검증 범위는 완료 보고에 명시한다.
  테스트 통과·장치 응답 성공·실제 패널 표시 확인은 구분한다.
- 지정된 에이전트나 모델을 사용할 수 없으면 이를 알리고 실제 수행 방식을 정확히 보고한다.
- 작업을 수행할 역량이 있는 후보 중 비용과 지연이 작은 에이전트를 우선한다. 탐색·구현·리뷰는
  서로 다른 역할이며 한도 초과를 이유로 역할을 바꾸지 않는다. 위임 비용이 더 크면 메인이 처리한다.
- 모델 사용량 한도 또는 모델 사용 불가가 확인되면 같은 모델을 반복 호출하지 않고 다음 가용 후보에
  인계한다. Codex의 후보 순서와 호출 제약은 아래 Codex 전용 절을 따른다. 동시 실행 수 제한은
  모델 교체로 해결하지 않고 기존 에이전트의 완료·재사용과 실행 수 조정으로 처리한다.
- 인계 전 기존 작업자의 중단 또는 완료를 확인하고 남긴 변경을 검사한다. 대체 작업자에게 목표,
  확정된 설계, 담당 파일, 완료·미완료 상태, 검증 결과와 남은 문제를 전달해 기존 변경을 보존한다.
  읽기 전용 역할과 권한·파일 소유 범위를 유지하고, 같은 파일을 두 작업자가 동시에 수정하지 않는다.
  전환 이유와 실제 사용 모델을 짧게 알린다. 모든 적합한 후보가 막히면 메인이 가능한 작업을 처리하고
  독립 리뷰 등 수행하지 못한 검증을 명시한다. 사용자의 중단 요청은 인계보다 우선한다.
- 진행 상황과 최종 결과는 한국어로 설명한다. 최종 보고에는 변경 내용, 실행한 검증과 결과,
  남은 문제를 적는다. 코드 식별자와 명령어는 원래 표기를 유지한다.

## 구현 원칙

- **Go답게 만든다.** 공식 Go 모듈 구조와 Code Review Comments를 따른다. 작은 패키지,
  구체 타입 우선, 소비자 쪽의 작은 인터페이스, 명시적 오류·취소·goroutine 생명주기를 사용한다.
- 센서·영상·폰트·USB 접근은 적합한 공개 라이브러리를 재사용한다. 제조사/참조 앱에서는 장치
  프로토콜과 전송 동작을 가져오고, 테마·센서 시스템 전체를 포팅하거나 독자 프레임워크를 만들지 않는다.
- **코드 작업에는 ponytail 스킬(`ponytail:ponytail`)을 쓴다.** 새 코드·리팩터링·의존성 선택에서
  최소 구현을 먼저 고른다. 위 Go 관례와 충돌하면 Go 관례가 우선한다.
- 코드 빌드와 배포 실행은 별도 검증한다. Windows 서명·평판, macOS 서명·공증과 동봉 런타임을
  고려한다. 미서명 개발본을 정식 배포본으로 취급하거나 OS 보안 기능 해제를 설치 전제로 삼지 않는다.

## 장치를 다룰 때

- **응답 읽기를 건너뛰지 않는다.** 응답을 무시하고 write만 하면 장치에 stale 응답이 쌓여
  이후 모든 전송이 타임아웃난다. 복구하려면 IN 큐를 비우고 sync를 다시 보내며,
  그래도 안 되면 USB를 뽑았다 꽂아야 한다. 실제로 한 번 이렇게 망가뜨렸다.
- 프로그램 시작 시 IN 큐를 먼저 비운다.
- 실측값은 **macOS + libusb 기준**이다. Windows에서는 flush 타임아웃을 다시 튜닝한다.
- PNG는 객체 타입이 아니라 **인코딩 결과가 8bit RGBA**여야 한다. Go 기본 PNG 인코더는 불투명
  RGBA를 RGB로 줄이므로 IHDR color type 6을 확인한다.

## 검증 명령

변경한 Go 파일에 `gofmt`를 적용하고, 코드 변경 후 다음 검사를 기본으로 수행한다.
Windows의 cgo·libusb 환경 준비는 `README.md`의 개발·검증 절을 따른다.

```bash
go vet ./...
go test ./...                 # 단위 테스트·golden 패킷 대조 — USB 장치 불필요
go build ./...
```

동시성 변경은 지원 환경에서 `go test -race ./...`를 추가한다. OS별 코드·외부 의존성 변경은
관련 OS의 빌드·실행을 검증하고, 실행하지 못한 검사는 사유와 함께 보고한다.
문서만 수정한 경우 코드 검사는 생략하고 diff·참조 경로·기존 규칙과의 일관성을 확인한다.
장치 검증 명령과 선택적 측정은 `README.md` 및 `DESIGN.md` 8절을 따르며,
보류된 장시간 실험을 자동 재개하지 않는다. 제품 데몬과 빌드·서명·패키징 파이프라인은 아직 없다.

## 라이선스

GPL-3.0-or-later. 장치 프로토콜 구현이
[turing-smart-screen-python](https://github.com/mathoudebine/turing-smart-screen-python)(GPL-3.0-or-later)의
파생이므로 같은 라이선스를 따른다. **새 소스 파일에 SPDX 헤더와 출처 고지를 유지한다.**

## 문서 규칙

`DESIGN.md`는 한 개발 사이클용이다. 구현이 끝나면 살아남을 규칙만 이 파일로 옮기고 삭제한다.
영구 worklog나 세션별 이력 문서는 만들지 않는다.

## Codex 전용 작업 규칙

- Codex의 도구·셸·경로에서 확인한 차이는 이 절에 기록한다. 공통 규칙은 이 파일의
  공통 절에, 구현 전 설계 결정은 `DESIGN.md`에 반영한다.
- 권한·샌드박스·사용 가능한 도구는 현재 세션이 제공하는 설정을 따른다. 한 세션의 권한이나
  승인 옵션을 이 파일에 영구 허용으로 적지 않는다.
- 이 파일에서 요구한 스킬은 현재 Codex 환경의 목록과 실제 경로를 확인한다. 없으면
  사용했다고 주장하지 않는다. 해당 스킬이 필요한 작업의 제약을 알리고, 독립적으로 가능한
  문서·주석 정리는 진행한다.
- 이 Windows 환경에서는 Go가 PATH에 없어도 `C:\Program Files\Go\bin\go.exe`에 설치되어
  있었다. 먼저 `Get-Command go`로 확인하고, 실패하면 해당 경로의 존재를 확인해 호출한다.
  PowerShell 예: `& 'C:\Program Files\Go\bin\go.exe' test ./...`.
  명령 탐색 실패만으로 Go 미설치나 테스트 불가능을 단정하지 않는다.

### 서브에이전트 모델 대체 순서

아래는 이 프로젝트의 후보 우선순위이며 모델별 한도가 독립적이라는 보장은 아니다.
호출 시 현재 세션에 실제로 제공되는 모델·역할을 확인하고, 미제공 또는 한도 소진 후보는 건너뛴다.

| 유지할 역할 | 기본 모델 → 대체 후보 순서 |
| --- | --- |
| `spark_explorer`의 좁은 읽기 전용 탐색 | `gpt-5.3-codex-spark` → `gpt-5.6-luna` → `gpt-5.6-sol` → `gpt-5.6-terra` |
| `spark_worker`의 범위가 명확한 구현 | `gpt-5.3-codex-spark` → `gpt-5.6-luna` → `gpt-5.6-sol` → `gpt-5.6-terra` |
| `reviewer`의 독립적인 읽기 전용 검토 | `gpt-5.6-terra` → `gpt-6-astra`의 별도 리뷰 에이전트 |

- 두 Spark 역할은 같은 모델을 사용하므로 한쪽의 모델 한도 초과 후 다른 역할로 같은 모델을
  재호출하지 않는다. 오류가 계정 전체 한도를 명시하면 다른 모델도 가능하다고 가정하지 않는다.
- 고정 모델을 쓰는 custom agent에 다른 모델을 강제로 지정하지 않는다. 해당 모델과 역할을 지원하는
  대체 custom agent가 있으면 사용하고, 없으면 모델 지정이 가능한 `default` 에이전트에 원래 역할의
  지침·제약·담당 범위를 명시한다. 대체 에이전트를 원래 Spark나 reviewer로 실행했다고 보고하지 않는다.
- 현재 `spawn_agent`에서 모델을 명시할 때는 `fork_turns: "none"` 또는 허용된 양의 정수 문자열을
  사용한다. 전체 이력 상속과 모델 override의 조합을 피하고, 인계에 필요한 문맥을 직접 제공한다.
  실제 도구 스키마가 바뀌면 그 제약을 우선한다. 모델 전환으로 권한을 확대하지 않는다.
- 리뷰 대체에서도 독립성과 읽기 전용을 유지한다. 비용 절약을 이유로 중요한 리뷰를 단순 구현 역할에
  넘기지 않는다. 이 규칙은 메인이 전환을 수행하는 지침이며 플랫폼의 자동 fallback 설정은 아니다.
