# CLAUDE.md — turzx-control

이 파일은 **Claude 전용 진입점과 지침**만 담는다.

- 작업 전에 [AGENTS.md](AGENTS.md)를 끝까지 읽고 프로젝트 공통 규칙과 읽기 순서를 따른다.
  `Codex 전용 작업 규칙` 절은 Claude에 적용하지 않는다.
- 프로젝트 맥락·명령·구현 원칙·장치 제약·라이선스·문서 규칙은 `AGENTS.md`를 원본으로 유지한다.
  공통 규칙을 이 파일에 복제하지 않는다. Claude 전용 동작·도구 차이만 여기에 추가한다.
- 현재 별도로 추가된 Claude 전용 작업 규칙은 없다.
